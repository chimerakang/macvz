# MASTER_TASKS — MacVz Development Plan

Source of truth for the phased roadmap. Phases map to GitHub **Milestones**; tasks map to GitHub **Issues**. Regenerate with `/task-sync`.

> **Strategy:** MacVz is a *node-layer* project — a Virtual Kubelet provider that runs OCI workloads as native micro-VMs on Apple Silicon via `apple/container`. We do **not** build a control plane. See [README](../README.md).
>
> **CRI feasibility:** A separate `develop` track is evaluating whether MacVz
> can also become a CRI runtime path for kubelet/k3s. The current strategy does
> not change until the Phase 0-2 risks are proven. See
> [CRI_FEASIBILITY.md](CRI_FEASIBILITY.md).

## Phase Overview

| Phase | Title | Goal | Status |
| --- | --- | --- | --- |
| P0 | Scaffolding & Foundations | Buildable Go project: module, layout, CLI skeleton, CI, build tooling | ✅ Complete |
| P1 | Runtime Integration | Drive `apple/container` from Go on a single Mac: micro-VM lifecycle, logs/exec, density benchmark | ✅ Complete |
| P2 | Virtual Kubelet Provider MVP | Mac registers as a k8s virtual node and runs real Pods as micro-VMs | ✅ Complete |
| P3 | Cross-host Mesh Networking | WireGuard mesh + Pod IPAM so Pods across Macs communicate and Services resolve | ✅ Implemented |
| P4 | Hardening & Beta | Metrics, volumes, image arch/Rosetta, mTLS/RBAC, signing/notarization, multi-node e2e | ✅ Complete |
| P5 | Privileged Networking & Full Data Plane | Make cross-Mac Service traffic work end-to-end and remove manual sudo from day-to-day operation | ⬜ Planned |
| P6 | Kubernetes Workload Compatibility | Support common Deployment-era Kubernetes primitives needed by real applications | ⬜ Planned |
| P7 | Multi-node Operations | Make Mac node bootstrap, joining, diagnostics, and removal repeatable | ⬜ Planned |
| P8 | Real App Validation | Run useful public and project-specific Kubernetes applications on MacVz | ⬜ Planned |
| P9 | Production Hardening | Improve recovery, resource accounting, packaging, upgrades, and long-running reliability | ⬜ Planned |

## Milestone Acceptance Criteria

- **P0** — `go build ./...`, lint, and tests are green in CI.
- **P1** — Go boots an Alpine micro-VM in seconds; `logs`/`exec` work; per-host concurrent-VM ceiling and per-VM RAM overhead are measured and recorded.
- **P2** — `kubectl run alpine --image=alpine --restart=Never -- sleep 3600` lands a micro-VM on the Mac; `kubectl logs`/`exec` work; node shows in `kubectl get nodes`. Operator-facing run/verify/cleanup steps and expected output are documented in [docs/P2_SMOKE_TEST.md](P2_SMOKE_TEST.md); RBAC and manifests under [deployments/](../deployments/).
- **P3** — A Service backed by Pods on two different Macs is reachable through normal Kubernetes networking.
- **P4** — Multi-node e2e suite green; signed/notarized `macvz-kubelet` build; volumes + image-arch handling supported.
- **P5** — Two-Mac e2e passes with `mesh.enabled: true` and `podNetwork.enabled: true`; Pod-to-Pod and Service traffic cross the WireGuard data plane; operators no longer need to start the main kubelet process with manual sudo.
- **P6** — A multi-Deployment application using ConfigMaps, Secrets, ServiceAccounts, probes, and image pull credentials can roll out and recover through normal Kubernetes controllers.
- **P7** — A new Mac can join the MacVz node pool through a documented bootstrap flow; existing nodes can be drained, diagnosed, and removed without manual cleanup.
- **P8** — At least one public Kubernetes application and one CBB-compatible subset run on MacVz and expose a browser-visible service.
- **P9** — Long-running soak tests survive kubelet/helper restarts, orphan cleanup works, resource usage remains bounded, and release artifacts can be installed, upgraded, rolled back, and removed.

## CRI Feasibility Track

This track is intentionally separate from P0-P9 because it may change the main
architecture from Virtual Kubelet provider to kubelet CRI runtime integration.

| Phase | Title | Status |
| --- | --- | --- |
| CRI-P0 | Map `apple/container` and MacVz runtime surfaces to CRI, name hard blockers | ✅ Complete |
| CRI-P1 | Build a minimal CRI server skeleton that answers kubelet `Status` | ✅ Complete |
| CRI-P2 | Spike Pod sandbox lifecycle over `apple/container` | ✅ Complete |
| CRI-P3 | Run a single-container Pod through the CRI adapter | ✅ Complete |
| CRI-P4 | Implement CRI ImageService pull/list/status/remove | ✅ Complete |
| CRI-P5 | Integrate CNI/Pod networking lifecycle | ✅ Complete |
| CRI-P6 | Implement logs, exec, attach, port-forward, and stats surfaces | ✅ Complete |
| CRI-P7 | Validate volumes, projected data, probes, and restart recovery | ✅ Complete |
| CRI-P8 | Harden k3s compatibility, install, cleanup, and soak behavior | ✅ Complete |
| CRI-P9 | Make the route-two go/no-go decision and migration plan | ✅ Complete (no-go for replacement; experimental side path) |
| CRI-P9 follow-up (#82) | Multi-container Pod feasibility: pause-VM shared-netns spike | ✅ Complete (blocked on missing `apple/container` primitive; flag-gated adapter path ready) |
| CRI-P9 follow-up (#83) | Real-hardware CRI-socket soak run and published report | ✅ Complete for crictl/socket soak; real kubelet/k3s in-loop soak still open |
| CRI-P9 follow-up (#84) | Host-namespace workload feasibility / honest scheduling exclusion | ✅ Complete (taint/label opt-in scheme + loud RunPodSandbox backstop) |
| CRI-P9 follow-up (#85) | Real kubelet/k3s fixture deployment and multi-day in-loop soak | 🟡 Harness/fixture/runbook built (`test/e2e/cri-k3s/k3s-inloop.sh`, `docs/CRI_K3S_INLOOP_REPORT.md`); gated live run operator-pending |
| CRI-P9 follow-up (#86) | Unblock honest multi-container Pods when `apple/container` exposes shared sandbox namespaces | 🟡 Capability re-verified absent (1.0.0); adapter-side honest path prepared behind `--experimental-multi-container` (join-in-sandbox-VM, one Pod IP, leak-free, owner-first drain guarded); blocked on runtime primitive with stable sandbox namespace lifetime |
| CRI runtime feasibility (#87) | Validate `apple/containerization` `LinuxPod` as the route-C Pod sandbox backend | 🟡 C0/C1 promising, but C2 proves post-create container hotplug unsupported; route C remains limited/experimental |
| CRI-C1 (#88) | Minimal LinuxPod two-container shared-namespace PoC | ✅ Complete (live LinuxPod two-container localhost/exec/stats/stop-order run passed; vmnet/PodIP probe remains a later network gate) |
| CRI-C2 (#89) | LinuxPod kubelet ordering and post-create container probe | ✅ Complete (live probe returns `unsupported: "hotplug not supported"`; all containers must be registered before `pod.create()` or use explicit stop/recreate fallback) |
| CRI-C3 (#90) | Decide LinuxPod backend limits and next sandbox strategy | ✅ Complete (route C stays experimental; no helper daemon/full CRI claim until hotplug boundary is proven or a limited model is explicitly accepted) |
| CRI-C4 (#91) | Probe LinuxPod HotplugProvider boundary on current apple/containerization | ✅ Complete (provider can be installed/called and USB mass-storage attach succeeds, but public APIs do not yield a deterministic guest block path for late rootfs mount) |
| CRI-R0 (#92) | Research Pod VM runtime architecture for full kubelet semantics | ✅ Complete (thin LinuxPod wrapper rejected as main path; target Pod VM runtime architecture and R1 device-discovery PoC defined) |
| CRI-R1 (#93) | Guest-side hotplug device discovery PoC for Pod VM rootfs attachments | ✅ Complete (host VZ USB attach succeeds, but guest observes no new USB/SCSI/block device; pivot to NBD or guest-side rootfs exposure) |
| CRI-R2 (#94) | Evaluate NBD and guest-side rootfs exposure fallback for Pod VM runtime | ✅ Complete (NBD pre-create rootfs identity selected for the next tiny PoC; guest-side rootfs staging remains the long-term kubelet-ordering answer) |
| CRI-R3 (#95) | NBD-backed pre-create rootfs identity PoC for LinuxPod containers | ✅ Complete (two NBD-served rootfs images boot as predeclared LinuxPod containers; guest virtio-block mount evidence and host-side EXT4 marker reads confirm identity) |
| CRI-R4 (#96) | Guest-side rootfs staging PoC for already-running Pod VM | ✅ Complete (post-create guest-side file staging works with explicit request identity; agent-created bind mount is not visible to a later exec in the predeclared utility container) |
| CRI-R5 (#97) | VM-agent process execution from staged guest rootfs | ✅ Complete (root-level VM process creation is unimplemented; `containerID=utility` can create/start a process but did not execute from the staged rootfs identity, outcome `processStartedButIdentityMismatch`) |
| CRI-R6 (#98) | Inspect `vminitd` container/rootfs process path after staged rootfs R5 | ✅ Complete (`id == containerID` reaches vminitd's new-container path and creates the container object; start still needs rootfs staged in the namespace vminitd can consume, outcome `vminitdContainerRootfsPathFound`) |
| CRI-R7 (#99) | Prove vminitd-visible rootfs staging for new-container start | ✅ Complete (utility-container staging addressed through `/run/container/utility/rootfs/...` still cannot start a new vminitd container; outcome `vminitdVisibleRootfsPrimitiveMissing`) |
| CRI-R8 (#100) | Design upstream-compatible vminitd rootfs/container primitive | ✅ Complete (selected two-stage `PrepareContainerRootfs` + `CreateContainer` primitive; next implementation is local experimental fork/patch plus upstream proposal, not production CRI wiring) |
| CRI-R9 (#101) | Prototype vminitd rootfs primitive launch with local patch | ✅ Complete (vminitd Copy prepared `/run/container/<id>/rootfs` and `createProcess(id == containerID)` succeeded; `startProcess` failed in `vmexec run` with ENOENT, outcome `vminitdContainerStartFailed`) |
| CRI-R10 (#102) | Instrument vminitd/vmexec start failure for late prepared rootfs | ✅ Complete (instrumented `vminit:macvz-r10` showed bundle/rootfs/init/dynamic linker all present; `vmexec` failed in console `/dev/ptmx` setup at `remove(ptmx)` with ENOENT, outcome `vmexecStartFailureExplained`) |
| CRI-R11 (#103) | Probe vmexec `/dev/ptmx` handling for late prepared rootfs start | ✅ Complete (local ptmx patch treated ENOENT as idempotent; `vminit:macvz-r11` advanced from `remove(ptmx)` to `open(/dev/null)` ENOENT, outcome `vmexecPtmxFixAdvancedToExec`) |
| CRI-R12 (#104) | Probe vmexec `/dev/null` setup for late prepared rootfs start | ✅ Complete (local devnull patch created char device `1:3` after OCI `/dev` tmpfs; `vminit:macvz-r12` advanced to userspace exit 127, outcome `vmexecDevNullFixAdvancedToExec`) |
| CRI-R13 (#105) | Make late-rootfs identity probe userspace runnable | ✅ Complete (R9 minimal rootfs now includes relative BusyBox applet symlinks; `vminit:macvz-r12` advanced to userspace exit 0, outcome `lateRootfsUserspaceAdvanced`) |
| CRI-R14 (#106) | Probe late-rootfs result visibility across vmexec mount namespace | ✅ Complete (process exits 0 after writing the result inside vmexec namespace, but vminitd cannot stat the prepared-rootfs result path; outcome `lateRootfsResultVisibilityExplained`) |
| CRI-R15 (#107) | Add verifiable identity evidence channel for late-rootfs launch | ✅ Complete (shared bind handoff verified late-rootfs identity through `/run/macvz-r9-evidence/<id>/macvz-r9-result`; outcome `vminitdRootfsPrimitiveLaunchSucceeded`) |
| CRI-R16 (#108) | Design production evidence/result handoff for late-rootfs containers | ✅ Complete (runtime-private `/run/macvz/containers/<id>/handoff` lifecycle, OCI bind mount, permissions, cleanup, and CRI mapping accepted; outcome `runtimeHandoffDesignAccepted`) |
| CRI-R17 (#124) | Prototype the LinuxPod late-rootfs runtime backend contract | ✅ Complete — turned the LinuxPod sandbox + R15/R16 handoff primitives into the smallest fakeable backend contract the Go adapter can call. New `pkg/runtime/linuxpod` defines `Backend` (Ping/CreatePod/PrepareContainerRootfs/CreateContainer/StartContainer/StopContainer/RemoveContainer/Status/Cleanup) with classified sentinel errors, an NDJSON wire protocol (`protocol.go`), a connection-per-call `HelperClient` (`client.go`), a helper-side `Serve` dispatcher (`server.go`), and an in-process `FakeBackend` (`fake.go`) that models one shared-namespace Pod VM, late-binding container creation after the pod is running, and rootfs identity verification reusing the production exact-match `runtime.HandoffMeta.Verify`. A Foundation/Darwin-only Swift helper stub (`test/e2e/cri-linuxpod-helper`, `swift build` green) serves the same protocol over a unix socket with a mirrored in-memory model (`Ping`→`simulated=true`). `macvz-cri` gains an explicit gate `--experimental-linuxpod-backend` + `--linuxpod-helper-socket` (off by default; loud startup handshake; apple/container serving path unchanged). **Evidence:** hermetic tests run the required ordering — CreatePod → app create/start → **late** sidecar create/start after app running — directly against the fake and over the wire, asserting shared `SandboxNamespace`, `LocalhostReachable`, sidecar `identityVerified=true`, `CreatedAfterPodRunning`, both stop orderings, idempotent remove, and a `Cleanup` leaving no stale state; plus identity-mismatch → `ErrIdentityUnverified` non-Running, and gate validation/handshake tests. Gated `TestSwiftHelperStubContract` (`MACVZ_LINUXPOD_HELPER=1`) and a live run proved the Go↔Swift contract and the real `macvz-cri` handshake over a real socket. `go build ./...`, `go vet ./...`, `go test ./pkg/criserver ./pkg/runtime ./cmd/macvz-cri` and full `go test ./...` green. Report `docs/CRI_RUNTIME_R17_LINUXPOD_BACKEND_REPORT.md`. Non-goals honored (no production claim, no k3s in-loop, no Service/DNS/cross-node net, no Attach/PortForward, apple/container backend and VK path unchanged). Outcome `linuxpodBackendContractPrototyped`. |
| CRI-L2 (#127) | Wire macvz-cri lifecycle serving to the LinuxPod backend | ✅ Complete — `--experimental-linuxpod-backend` now *serves* the CRI lifecycle through `pkg/runtime/linuxpod.Backend` instead of only the startup handshake. New `pkg/criserver/linuxpod_service.go` is a SEPARATE `RuntimeService`/`ImageService` (`LinuxPodService`); `cmd/macvz-cri` registers it via `serveLinuxPod` when the gate is on, leaving the shipped apple/container `Server` path untouched and unreachable in that mode. Mapping: RunPodSandbox→`CreatePod` (persists `LinuxPodNamespace`); CreateContainer→`PrepareContainerRootfs`+`CreateContainer` (late-binds, deterministic `expectedIdentity` per CRI id, persists `store.LinuxPodContainer{BackendContainerID,RootfsToken,Expected/Observed/IdentityVerified}`); StartContainer→identity-gated `StartContainer` (mismatch ⇒ `FailedPrecondition`, marked Exited, never silently Running); Stop/Remove/Status/PodSandboxStatus/StopPodSandbox/RemovePodSandbox→backend lifecycle/`Cleanup`; restart reconcile via `RecoverContainers` that does **not** trust persisted identity (a backend that lost the Pod ⇒ container reconciled to Exited). Persisted sandbox/container store metadata carries the backend mapping for restart recovery. **Evidence:** hermetic `pkg/criserver/linuxpod_service_test.go` drives the kubelet ordering through the CRI server APIs against `FakeBackend` — app create/start→Running, **late** sidecar create/start after app Running, both containers in one shared sandbox namespace with `identityVerified=true`, identity-mismatch stays non-Running (`FailedPrecondition`), idempotent stop/remove/sandbox-cleanup leaving no stale backend state, and restart recovery reconciling without trusting stale identity. `go build ./...`, `go vet ./...`, full `go test ./...`, and `make build` green. Non-goals honored (apple/container backend kept; Pod networking is CRI-L3 #128; logs/exec/stats honest-unsupported per helper capabilities, CRI-L4 #129). Outcome `linuxpodCRILifecycleServingWired`. |
| CRI-L0 (#125) | Umbrella: LinuxPod-backed CRI runtime phase | 🟡 In progress — turns the #124 contract prototype into a kubelet-drivable LinuxPod-backed CRI path. **Done & live-verified:** #127 serving wired AND now proven end-to-end against the **real** Apple Containerization helper — `TestLiveLinuxPodServingThroughHelper` (gated, real `linuxpod-helper`) drives a CRI gRPC client → `macvz-cri --experimental-linuxpod-backend` → real helper → **real LinuxPod micro-VM**: RunPodSandbox boots the VM, app create/start→Running (identity verified), **late** sidecar create/start after app Running, app+sidecar share one namespace, stop+RemovePodSandbox clean. This closed the **#127↔#126 integration gap** the #130 crictl E2E found (`StartContainer` failed via the serving path): root cause was `sethostname()` EINVAL — the pod hostname (empty from CRI) and the container hostname (`macvz-<64-hex-id>` > HOST_NAME_MAX) were invalid only on the real-id serving path; fixed by clamping both in the helper (`Backend.swift`). #126 real helper drives the full lifecycle live (`simulated:false`). #128 Pod networking integrated and live-reachable (IPAM+`podnet` attach, honest `NetworkReady`/Pod-IP/VM-IP verbose status, detach/release, restart recovery, default-route-unchanged proof; external host-to-Pod Pod IP HTTP passed via `10.244.102.0/24 -> 192.168.1.122`). `ImageService.ImageFsInfo` (the other #130 crictl blocker) now implemented on `LinuxPodService`. **Remaining:** #129 real-helper logs/exec/stats (capabilities still `false`; contract+fake+stub done) and #130 the literal **kubelet/k3s** in-loop run on a real cluster (harness ready; no cluster in this environment). The umbrella stays open until those close with live kubelet/k3s evidence; shipped Virtual Kubelet and apple/container CLI paths untouched. Outcome `linuxpodCRIRuntimePhaseValidated` (pending the kubelet/k3s cluster run). |
| CRI-L1 (#126) | Real LinuxPod helper (Apple Containerization lifecycle) | ✅ Complete — proven live on real micro-VMs. **Built** a real `linuxpod-helper` Swift target (`test/e2e/cri-linuxpod/Sources/LinuxPodHelper/`, in the package that wires Apple Containerization) that speaks the same `pkg/runtime/linuxpod` NDJSON protocol (v3) as the stub but drives a real `LinuxPod` VM + the R9/R16 late-rootfs identity primitive (`Ping` reports `simulated:false`); the stub is retained for hermetic tests. Op mapping: `CreatePod` boots a LinuxPod kept up by a long-lived holder container; `PrepareContainerRootfs` stages a busybox rootfs + the expected identity into the running VM via the vminitd `Copy` primitive; `CreateContainer` late-binds a vminitd process (flagging `createdAfterPodRunning` for the late sidecar); `StartContainer` gates Running on **host-side** identity verification through a bind-mounted handoff evidence channel (CRI-R16) and records the process's real `/proc/self/ns/net` inode as genuine shared-namespace evidence; `Stop/Remove/Cleanup` tear down the process/rootfs/VM with no residual state; the kubelet surfaces (`ContainerLogPath`/`ExecSync`/`ContainerStats`) advertise `false` and return `Unsupported` (owned by CRI-L4 #129); failures classify through the existing wire codes (AC6). **Live evidence (Apple Silicon, macOS 26, Swift 6.2.1, Apple Containerization `6b7b42c` + R10/R11/R12 vmexec patches):** `TestRealLinuxPodHelperLifecycle` **PASS** — `CreatePod` booted a real VM; app prepared/created/started; sidecar **created after the app was Running** (`createdAfterPodRunning=true`) and started; app+sidecar share one real net namespace inode `net:[4026531840]`; each identity verified (`observed==expected`) before Running; Cleanup `podRemoved=true staleState=false` and idempotent; kubelet surfaces returned `ErrUnsupported`. Provisioning gates were fixed, not papered over (VM entitlement → codesign; vmexec ENOENT → R11/R12 ptmx+`/dev/null` fixes; stale `initfs.ext4` busted). **Other verification:** `swift build --product linuxpod-helper` green; stub still builds; `go build`/`go vet`/`go test ./...` green (contract unchanged, `ProtocolVersion` stays 3). Added gated `TestRealLinuxPodHelperLifecycle` (`make cri-linuxpod-helper-real`). Report `docs/CRI_RUNTIME_L1_REAL_HELPER_REPORT.md`. Outcome `realLinuxPodHelperLifecycleProven`. |
| CRI-L3 (#128) | Pod networking for LinuxPod-backed sandboxes | ✅ Complete for CRI-level LinuxPod podnet — attaches MacVz Pod networking to LinuxPod-backed CRI sandboxes, reusing the shipped primitives (`network.PodIPAM` + `podnet.Router`) rather than a CRI-only path. **Contract:** `pkg/runtime/linuxpod` gained `PodStatus.SandboxAddress` (the Pod VM's host-reachable vmnet address; `""`=not-yet-discovered) and a `Backend.PodStatus(podID)` query wired through the NDJSON op/dispatch/client and `FakeBackend` (controllable address + `SandboxAddressReadyAfter` latency model); `ProtocolVersion` 2→3 and the Swift helper stub mirrors it. The real `linuxpod-helper` now has an explicit `--vmnet` mode that allocates one `VmnetNetwork` interface per Pod, attaches it to the `LinuxPod`, reports the interface IPv4 as `sandboxAddress`, and releases it on create failure/Cleanup; default helper runs leave vmnet off. **Integration** (`pkg/criserver/linuxpod_network.go`): `LinuxPodService` gained `PodNetwork`+`IPAM`; because the Pod VM boots at `RunPodSandbox` (unlike the apple/container per-container path), the host binat path attaches there — reserve Pod IP (keyed by `namespace/name`, stable across recreation) → discover the sandbox address by polling `PodStatus` → program the rule → persist; `PodSandboxStatus` reports the Pod IP (via shared `toCRIStatus`) only after attach and verbose status now exposes `networkAttached`/`podIP`/`vmIP`/`interface`; `Status.NetworkReady` true only when IPAM+host path wired; `StopPodSandbox` detaches and retains the IP, `RemovePodSandbox` detaches+releases; `RecoverNetwork` re-reserves and re-attaches survivors after restart without leaking. **Diagnostics:** `LinuxPodNetworkError{Class}` distinguishes `helper`/`ip-reservation`/`address-discovery`/`route-pf` and maps to `Unavailable`/`ResourceExhausted`/`Unavailable`/`Internal` via `GRPCStatus()`. **Default route:** never touched — `podnet.Router` only removes the scoped `-ifscope` vmnet default, asserted by `TestAttachDetachPreservesGlobalDefaultRoute`. **Evidence:** hermetic `pkg/criserver` `TestLinuxPodNetworkAttachOnRunPodSandbox`/`DisabledRunsWithoutPodIP`/`AddressDiscoveryLatencyTolerated`/`FailureClasses`/`DetachAndReleaseOnStopRemove`/`RestartRecovery`; `pkg/runtime/linuxpod` `TestPodStatusAddressDiscoveryOverWire`; `pkg/network/podnet` default-route test; real-helper `--vmnet` sandboxAddress gate passed on `test@192.168.1.122` (`192.168.66.2`) with default route unchanged (`192.168.1.1`/`en0`). **Gated/live:** full `macvz-cri` podnet attach passed on `test@192.168.1.122` (`NetworkReady=true`, Pod IP `10.244.102.2`, `vmIP=192.168.66.2`, clean detach, default route unchanged). Packet reachability also passed: held a real busybox `httpd` in the LinuxPod, verified local route `10.244.102.0/24 -> 192.168.1.122`, and `curl http://10.244.102.2:8080/` returned HTTP 200 `macvz-linuxpod-podnet-ok`; remote cleanup preserved default route. Pod-to-host callback remains an optional diagnostic, not a #128 gate. `swift build --product linuxpod-helper`, `go build ./...`, `go vet ./...`, `go test ./...`, `make build` green. Report `docs/CRI_RUNTIME_L3_LINUXPOD_PODNET_REPORT.md`. Non-goals honored (host default route + provider networking unchanged; full Services/DNS deferred; no production claim). Outcome `linuxpodPodNetworkingLiveReachable`. |
| CRI-L4 (#129) | Logs/exec/stats kubelet surfaces | ✅ Complete for the experimental adapter surface — capability negotiation (`HelperInfo.Capabilities`) + `ContainerLogPath`/`ExecSync`/`ContainerStats` with `ErrUnsupported` for unbacked surfaces; fake + Swift stub implement them (simulated, flagged), and `LinuxPodService` now exposes the CRI RPCs: absolute log-path propagation + `ReopenContainerLog`, `ExecSync`, `ContainerStats`/`ListContainerStats`/`PodSandboxStats`/`ListPodSandboxStats`, with unsupported surfaces mapped honestly to `Unimplemented` or attributes-only stats. **Evidence:** hermetic backend surface tests, CRI service regression tests, gated Go↔Swift contract test, `go test ./pkg/criserver ./pkg/runtime/linuxpod ./cmd/macvz-cri` green. **Still future:** real Apple Containerization-backed logs/exec/stats are tracked in #133; interactive/streaming Exec in #132; Attach/PortForward in #131. Outcome `linuxpodKubeletSurfacesDefined`. |
| CRI-L5 (#130) | Kubelet/k3s in-loop validation + recovery audit | ✅ Live PASS for the CRI-L5 in-loop gate — the real `kind-macvz61` kubelet drives the genuine LinuxPod backend (`simulated=false`) on `test@192.168.1.122` through Deployment Available, shared localhost proof, Pod IP `10.244.102.2`, `kubectl logs`, non-interactive `kubectl exec`, `kubectl port-forward`, ClusterIP Service reachability from a Linux-node probe, bounded short soak, cleanup audit with no residual LinuxPod VM/container/rootfs/handoff state, and unchanged default route (`192.168.1.1` via `en0`). Final full-pass evidence: `/tmp/cri-linuxpod-v5-cleanup-20260624220501` / harness diagnostics in `/var/folders/0v/kl23hnt5017297mrg62rqwqc0000gn/T/macvz-cri-linuxpod-inloop.PXs5w9qt52/out`; additional hooked retest `/tmp/cri-linuxpod-v5-restart-cri-20260624223422` proved identity, `macvz-cri` restart recovery (same Pod UID), and RSS growth within bound. This closed the remaining blockers by signing the protocol-v5 helper with the virtualization entitlement, adding missing BusyBox applets (`seq`, `tail`), making writable materialized volumes guest-writable, merging workload stdout into the reliable CRI log stream for this vminitd path, and cleaning LinuxPod helper/VM state at `StopPodSandbox` because kubelet may defer `RemovePodSandbox`. Helper restart recovery now passes via explicit fail-fast/recreate semantics: the adapter's backend reconciler marks helper-lost sandboxes `BackendLost`/NotReady, `RunPodSandbox` discards stale NotReady records for same-Pod recreation, kubelet recreates the sandbox, and `kubectl exec` works after recreation. Latest live evidence `/tmp/cri-linuxpod-v5-helper-recovery-20260625003511` passed restart-helper, restart-cri, identity, RSS, cleanup, and route audit hooks with restartCount bounded and default route unchanged. The shipped Virtual Kubelet and apple/container paths stay untouched. Report `docs/CRI_LINUXPOD_L5_INLOOP_VALIDATION_REPORT.md`. |
| CRI-L4 follow-up (#131) | Attach/PortForward for the LinuxPod backend | ✅ Complete — added the interactive surfaces #129 scoped out, as capability-negotiated contract surfaces (the AC1 first option). `Capabilities.Attach`/`Capabilities.PortForward` advertised in `Ping`; new `Backend.Attach(AttachRequest)→AttachResponse` and `PortForward(PortForwardRequest)→PortForwardResponse` with NDJSON ops `Attach`/`PortForward` (introduced in protocol v5; current head is ProtocolVersion=6 after #138). Both are capability-gated (`ErrUnsupported` when the capability is false) and validate the target — Attach requires a Running container (`ErrInvalid` otherwise, `ErrContainerNotFound` for an unknown ref); PortForward requires a known pod (`ErrPodNotFound`) and in-range ports (`ErrInvalid`). The Go `FakeBackend` and the Swift stub both implement them in **simulated** form (responses flagged `Simulated`; real VM-internal stream/byte-forward plumbing is the explicit #131 non-goal), kept in lock-step. **Evidence:** hermetic `surfaces_test.go` (`TestAttach/PortForwardSupportedAndGated`: supported + over-pipe + unsupported + validation paths) and the gated Go↔Swift `TestSwiftHelperStubContract` now negotiate Attach/PortForward over a real unix socket (`MACVZ_LINUXPOD_HELPER=1`, swift build green). `go build ./...`, `go vet ./...`, full `go test ./...` green. Backend-contract scope only (the CRI streaming-server URL wiring is separate); real stream plumbing deferred to a production helper. Outcome `linuxpodAttachPortForwardNegotiated`. |
| CRI-L4 follow-up (#133) | Back logs/exec/stats with real Apple Containerization data | ✅ Complete — the real `linuxpod-helper` (#126) now backs the kubelet surfaces with **measured** Pod-VM data instead of advertising them `false`; capabilities flip to `["logs":true,"exec":true,"stats":true]` and `simulated` stays `false`, so the `LinuxPodService` serving path (which already routes these RPCs to the backend by capability, #129) serves real surfaces. **Implementation (`Backend.swift`, public Containerization API only):** **stats** via `Vminitd.containerStatistics(.all)` — memory working-set from `memory.usageBytes`, `CPUUsageNanoCores` derived from two `cpu.usageUsec` samples 100 ms apart, `simulated=false`; **exec** via the low-level vminitd exec path (`createProcess` with a fresh id + the container's id and a full OCI spec), capturing real stdout/stderr over host vsock ports (`Transport.captureVsockStream`) and the real `waitProcess` exit code; **logs** by wiring the container's stdout/stderr to host vsock ports at `createProcess` and streaming them into the CRI log file (`<rfc3339nano> <stream> F <msg>`) via detached tasks cancelled on remove; `ContainerLogPath` returns `format=cri`. Helper `protocolVersion` was aligned to the then-current `ProtocolVersion=5`; current head is ProtocolVersion=6 after #138 and both Swift helpers now advertise 6. **Live evidence (gated `TestRealLinuxPodHelperLifecycle`, Apple Silicon, real micro-VM):** PASS — `exec echo macvz-exec-ok -> exit=0 stdout="macvz-exec-ok"` (and `exit 7` passthrough); `stats memWorkingSetBytes=212992 simulated=false`; `logs -> logger.cri.log: "…Z stdout F macvz-log-marker"`. `swift build --product linuxpod-helper`, `go build ./...`, `go vet ./...`, hermetic `go test ./...` all green (Go contract + simulated stub unchanged; stub stays `simulated=true`). Report `docs/CRI_RUNTIME_L4_REAL_SURFACES_REPORT.md`. Non-goals honored (interactive Exec #132 / Attach·PortForward #131 separate; apple/container + VK paths untouched). Outcome `linuxpodKubeletSurfacesMeasured`. |
| CRI-L6-1 (#135) | LinuxPod soak/churn harness for kubelet/helper/netd churn | ✅ Complete — added `test/e2e/cri-k3s/linuxpod-soak.sh` (`make cri-linuxpod-soak`), the soak sibling of `linuxpod-inloop.sh` (#130). It drives the same real kubelet/k3s topology and `fixtures/linuxpod-workload.yaml`, but loops restarts and lifecycle churn over `MACVZ_SOAK_ITERATIONS` iterations, round-robin over the enabled `MACVZ_SOAK_CHURN_MODES` (`rollout`, `cri`, `helper`, `netd`). Each iteration records Pod UID, Pod IP, restartCount, adapter RSS, helper RSS (via new `MACVZ_HELPER_RSS_CMD`), and residual LinuxPod-state count to `soak-samples.csv`, and re-asserts the host default route is unchanged. It proves the #134 recovery acceptances: a `cri` restart preserves the Pod UID with no duplicate backend state (residual stays at the post-deploy baseline ceiling); a `helper` restart recovers live or via bounded kubelet recreate with `kubectl exec` + `logs` working afterward; a `netd` restart/reload (new `MACVZ_RESTART_NETD_CMD`) never mutates the default route and restores Service/Pod reachability; cleanup leaves zero residual VM/container/rootfs/handoff/network state. Inherits the #130 **honesty gate** (`MACVZ_LINUXPOD_BACKEND_EVIDENCE_CMD`): LinuxPod-backend claims skip loudly unless the Pod is proven non-simulated, and a churn mode whose hook is unset is dropped with a loud skip (never a silent pass). Failure output is tagged with the exact failed phase and points at per-iteration diagnostics paths. **Validation:** `bash -n` + `shellcheck -S warning` clean; plan-only mode (no `MACVZ_INTEGRATION`/`KUBECONFIG`) prints the runbook and exits 0. The shipped Virtual Kubelet and apple/container paths stay untouched. **Live evidence:** on `test@192.168.1.122`, `linuxpod-inloop` with CRI/helper restart hooks passed (`/tmp/macvz-live-inloop-hooks-202606260324`), short `linuxpod-soak` with `cri,helper` churn passed after waiting for real exec/log readiness (`/tmp/macvz-live-soak-hooks-fix-202606260326`), and `netd` reload churn passed via non-sudo helper-socket `reloadPolicy` (`/tmp/macvz-live-soak-netd-202606262219`). Post-#139 retest `/tmp/macvz-live-139-soak-fix-20260627210044` passed 6 iterations over `rollout,cri,helper`: rollout picks the newest Running Pod rather than stale Completed Pods, CRI duplicate checks wait for kubelet's transient stopped sandbox records to converge, helper-router restarts recovered live with the same Pod UID, adapter RSS grew `1344KB`, cleanup was zero-residual, and the default route stayed `192.168.1.1` via `en0`. All runs preserved the default route and cleanup left zero residual VM/container/rootfs/handoff/network state. |
| CRI-L6-2 (#136) | Harden stale LinuxPod CRI state cleanup + diagnostics | ✅ Complete — made stale-state handling explicit and diagnosable on top of the CRI-L5 (`888ce30`) `BackendLost`/reconcile/`NotReady`-discard machinery. **Diagnostic:** new read-only residual-state scanner (`pkg/criserver/linuxpod_diagnose.go`) classifies persisted sandbox/container records into machine-readable categories — `sandbox-ready-backend-{live,lost,unprobed,error}`, `sandbox-notready-retained`, `container-running-backend-{live,lost,unprobed,error}`, `container-created-retained`, `container-exited-retained`, `container-orphaned` — plus per-sandbox Pod-network residual (PodIP/VMIP/interface/attached/`ipReservedForKey`) and per-container materialized-mount residual (`hostPathExists`). Exposed in-process via `LinuxPodService.Diagnose` and as the scriptable `macvz-cri --diagnose-linuxpod` CLI (loads the stores read-only, verifies the helper protocol before probing when `--linuxpod-helper-socket` is given else reports liveness `unprobed`, prints indented JSON with a per-category `summary`, exits without serving). **Read-only guarantee:** the scan never mutates a record, IP reservation, or host route — proven by `TestLinuxPodDiagnoseBackendLost` (records stay Ready/Running after the scan) and `TestLinuxPodDiagnoseDoesNotTouchNetwork`. **Cleanup scope:** `TestLinuxPodDiagnoseReconcileDetachesOnlyLostSandbox` proves reconciling one backend-lost sandbox detaches only its key and leaves an unrelated sandbox's rule + IP intact; the host default route is never touched (`podnet.Router` scoped-default invariant). **Pod IP stability:** `TestLinuxPodPodIPStableAcrossBackendLostRecreate` proves a Pod keeps its IP across `BackendLost`→reconcile(NotReady)→same-Pod recreate (reservation keyed by `namespace/name`, released only at `RemovePodSandbox`, idempotent `Allocate`). **Evidence:** hermetic `pkg/criserver` diagnose suite (live/lost/notready/orphaned/unprobed/mounts) + `cmd/macvz-cri` diagnostic tests (store scan, helper handshake/probe, protocol-mismatch rejection); `go build ./...`, `go vet ./...`, `go test ./pkg/criserver/... ./cmd/macvz-cri/...` green. Docs `docs/CRI_LINUXPOD_L6_2_STALE_STATE.md` (fail-fast/recreate policy + how to interpret `BackendLost`). The shipped Virtual Kubelet and apple/container paths stay untouched. |
| CRI-L6-3 (#137) | LinuxPod multi-Pod concurrency + bounded resource growth | ✅ Complete — added `test/e2e/cri-k3s/linuxpod-multipod.sh` (`make cri-linuxpod-multipod`) and `fixtures/linuxpod-multipod-workload.yaml`, the concurrency sibling of `linuxpod-inloop.sh` (#130). It deploys a Deployment of N>=3 app+late-sidecar replicas onto the single MacVz CRI node so several concurrent LinuxPod micro-VMs run side by side under one adapter/helper, and proves the #137 acceptances: >=3 Pods reach Ready; each Pod gets a **unique** Pod IP from the node PodCIDR (uniqueness + PodCIDR membership asserted); the ClusterIP Service load-balances across >=N **distinct** Pod backends (per-Pod `/whoami` identity); every Pod is reachable on its **direct Pod IP**; per-Pod logs/exec/port-forward all work concurrently. Recovery under concurrency: a `macvz-cri` restart keeps **every** Pod UID with no doubled residual state (no duplicate/lost sandboxes); a `linuxpod-helper` restart yields bounded recreate with `kubectl exec` + shared-ns proof working on every Pod afterward (no stale Running-but-unusable Pod). It asserts **no duplicate pf/binat or helper-work** record via `MACVZ_LINUXPOD_DUP_AUDIT_CMD` (falls back to audit line-uniqueness), runs a scale-churn loop (`MACVZ_MULTIPOD_CHURN_CYCLES` up to `MACVZ_MULTIPOD_CHURN_HIGH`) proving Pod IPs stay unique and residual state + helper/process count return to baseline, and the soak tracks adapter RSS and helper/process count under concurrency against documented pass/fail thresholds (`MACVZ_INLOOP_RSS_GROWTH_KB`, new `MACVZ_MULTIPOD_PROC_GROWTH` via new `MACVZ_LINUXPOD_HELPER_PROC_CMD`). Inherits the #130 **honesty gate** per Pod: **every** replica must prove `simulated=false` via `MACVZ_LINUXPOD_BACKEND_EVIDENCE_CMD` or the LinuxPod acceptances skip loudly (never a silent pass). **Validation:** `bash -n` + `shellcheck -S warning` clean; `kubectl --dry-run=client` accepts the fixture; plan-only mode (no `MACVZ_INTEGRATION`/`KUBECONFIG`) prints the runbook and exits 0. The shipped Virtual Kubelet and apple/container paths stay untouched. **Live evidence:** on `test@192.168.1.122`, `/tmp/macvz-live-multipod-vmnet18-20260626230234` passed end-to-end with 3 genuine non-simulated LinuxPod Pods, unique `10.244.102.0/24` Pod IPs, shared namespace/logs/exec/port-forward, ClusterIP balancing to 3 distinct backends, direct PodIP reachability 3/3, CRI restart recovery with all Pod UIDs unchanged and residual state bounded (`96 -> 96` lines), helper restart recovery with exec/shared-ns proof on every Pod, duplicate audit clean, scale churn `3 -> 4 -> 3` returning residual state and helper/process count to baseline, RSS growth `32KB <= 98304KB`, cleanup zero residual VM/container/rootfs/handoff/network state, and default route unchanged (`192.168.1.1` via `en0`). The live node's `macvz-netd` policy was reloaded to `podNetwork.vmNetCIDRs: ["192.168.64.0/18"]` after Apple vmnet assigned `192.168.81.x`; repo defaults/docs now match that safer host-only vmnet range while default-route control remains out of scope. |
| CRI-L6 adoption (#138) | LinuxPod live-VM adoption after helper restart | 🟡 Adoption protocol implemented; true VM-handle reattachment remains blocked by public Containerization API. Replaces the fail-fast-only restart policy with an opt-in **adoption** path: a restarted helper consults a durable journal and either reports `Adopted:true` with live container statuses or `Adopted:false` with a diagnostic reason so the adapter immediately falls back to `BackendLost`/recreate (#136). **Contract (`pkg/runtime/linuxpod`, ProtocolVersion 5→6):** `Backend.Adopt(podID)→AdoptionResult{Adopted,Containers,Reason}`, `Capabilities.Adopt`, and `HelperInfo.Adoption{Supported,AdoptedPods,LostPods}` are wired through NDJSON client/server/dispatch; `ErrUnsupported` still means the backend does not implement the durable adoption protocol. **Modeled backend:** `FakeBackend` keeps the full reattach model (`SimulateHelperRestart`, durable journal, `VMSurvivesRestart`) and tests both preserved-VM and dead-VM outcomes. **Adapter:** `AdoptSandboxes` runs during startup recovery, and the periodic reconciler now also tries `Adopt` once when `PodStatus` reports a missing live handle, covering helper-only restarts while `macvz-cri` stays up; incomplete or false adoption funnels through the shared `markSandboxBackendLostLocked` fallback so stale Running-but-unusable Pods are never left behind. **Real Swift helper:** persists `adoption-journal.json` under its work root, preserves the work root across helper bootstrap, advertises `adopt:true`, returns `Adopted:true` for pods still live in the current helper process, and returns `Adopted:false` for journaled pods it cannot reacquire after restart because Containerization exposes VM creation but no lookup/reattach hook; cleanup removes journal entries even when the live pod handle is already gone. **Evidence:** hermetic `pkg/runtime/linuxpod/adopt_test.go`; `pkg/criserver` adoption tests including `TestLinuxPodServiceReconcilerAdoptsAfterHelperRestart`; Swift helper build green (`swift build --package-path test/e2e/cri-linuxpod --product linuxpod-helper`); `go test ./...` and `go vet ./...` green. Live probe on `test@192.168.1.122` after deploying the new test binaries confirmed `Ping` now reports `simulated=false`, `protocolVersion=6`, `capabilities.adopt=true`, `adoption.supported=true`, `lostPods=0`; an injected journal fixture reported `lostPods=1` after helper restart and `Adopted:false` with the Containerization lookup/reattach diagnostic, then `Cleanup` removed the journal and a restart returned `lostPods=0`; default route stayed `192.168.1.1` via `en0`. Report `docs/CRI_LINUXPOD_ADOPTION_DESIGN.md`. The shipped Virtual Kubelet and apple/container paths stay untouched. |
| CRI-L6-4 (#139) | Supervisor-backed LinuxPod helper adoption | ✅ Complete — true no-recreate helper restart via an **ownership inversion**. Because public Apple Containerization exposes `VirtualMachineManager.create(config:)` but no VM lookup/reattach hook, the live VM handle can never be reconstructed by a fresh process — so it is no longer reconstructed: it stays held by a per-Pod **supervisor** process that outlives the main helper. **Architecture (`test/e2e/cri-linuxpod/Sources/LinuxPodHelper`):** `linuxpod-helper` became a parent command with two subcommands. `serve` (default) is the **router** — it owns the public CRI socket, the durable `supervisor-journal.json`, and routing, and owns no VM. `supervise-pod` (hidden) is a **per-Pod supervisor** — it owns exactly one `LinuxPod`/`VZVirtualMachineInstance` by hosting the unchanged `LinuxPodBackend` and serving the same NDJSON protocol on a private socket; it `setsid()`s so a `SIGTERM`/`SIGKILL` to the router alone leaves the Pod VM running. Bare `linuxpod-helper --socket … --kernel …` still routes to `serve`, so the Go adapter, protocol, and the real-helper lifecycle test are unchanged. **Routing:** `CreatePod` spawns a detached supervisor and journals it (podID/socket/pid/startUnix/sandbox addr+ns); pod-scoped ops forward verbatim to that supervisor; `Adopt` reconnects to the journaled supervisor (reachable → `Adopted:true` + live containers; unreachable → `Adopted:false`, no error); `Cleanup` drives the supervisor's own VM/rootfs/interface teardown then terminates it and drops the journal entry, idempotently; `Ping` reports the startup adoption pass. **Startup adoption pass:** `RouterBackend.init` probes each journaled supervisor with `Ping` — reachable bumps `adoptedPods` and keeps the live client, unreachable bumps `lostPods` and drops the stale entry — so the real helper's `Adopt` now returns `Adopted:true` because a supervisor kept the VM alive, while missing/incomplete supervisors keep funneling through the shared `markSandboxBackendLostLocked` fallback (#136). A dead supervisor socket is handled as `EPIPE`/`EOF` (process-wide `SIGPIPE` ignore + `SO_NOSIGPIPE`), never a router crash. **Evidence:** hermetic `pkg/runtime/linuxpod/router_supervisor_test.go` (`TestSwiftRouterSupervisorAdoption`, gated `MACVZ_LINUXPOD_HELPER=1`) exercises the whole inversion **without booting a VM** by pointing `--supervisor-command` at the in-memory stub: create-via-router → kill router only → restart reports `adoptedPods=1`/`lostPods=0` and `Adopt` reattaches the running container → kill supervisor → `Adopt`→`adopted:false`, routed `Status` fails, `Cleanup` removes the journal entry idempotently. Live smoke on `test@192.168.1.122` with the signed real helper proved the VM-owning path: `CreatePod(pod-139-live)` spawned supervisor pid `15915`, returned `sandboxAddress=192.168.82.2`, and wrote `supervisor-journal.json`; restarting only the public router left the supervisor alive (PPID `1`), new `Ping` reported `adoptedPods=1`/`lostPods=0`, `Adopt` returned `Adopted:true`, routed `PodStatus` still saw the same Running Pod VM, `Cleanup` removed the supervisor/journal idempotently, and the default route stayed `192.168.1.1` via `en0`. Full kubelet/k3s validation on 2026-06-27 (`/tmp/macvz-live-139-inloop-20260627204244/run-pass`) passed the LinuxPod in-loop suite with CRI/helper restart, identity, RSS, cleanup, and route hooks; a focused manual router-restart check confirmed the same Pod UID `ea235936-a1a7-4af1-8c3f-621074d3187e`, Pod IP `10.244.102.2`, and restartCount `0` before/after `adoptedPods=1` / `lostPods=0`, with final cleanup empty and default route unchanged. `swift build --product linuxpod-helper`, `TestSwiftHelperStubContract`, `go test ./...`, and `go vet ./...` green; the existing gated `TestRealLinuxPodHelperLifecycle` now exercises router→`supervise-pod`. Report `docs/CRI_LINUXPOD_ADOPTION_DESIGN.md` (`#139` section). Non-goals honored: no private Apple APIs, no VK/apple-container path changes, no host route/pf policy changes. |

## CRI Implementation Track

The R-series research work proved the late-rootfs route and accepted the
production handoff design. The I-series implementation track turns that evidence
into an experimental LinuxPod-backed CRI runtime path. It remains separate from
the shipped Virtual Kubelet node-provider path until kubelet/k3s validation is
strong enough to change the product posture.

| Phase | Title | Goal | Issues | Status |
| --- | --- | --- | --- | --- |
| CRI-I1 | Runtime Handoff Foundation | Build the runtime-private path, metadata, and mount-collision foundation needed by all later work | #109, #110, #111 | ✅ Complete (#109 path lifecycle helper, #110 runtime metadata, #111 mount-collision guard) |
| CRI-I2 | Late Rootfs Launch Primitive | Move the R15 handoff proof into the experimental runtime launch primitive and gated integration coverage | #112, #113, #114 | ✅ Complete (#112 OCI handoff bind-mount injection, #113 identity file/handoff writer contract, #114 gated live handoff-launch integration) |
| CRI-I3 | CRI Lifecycle Integration | Map Create/Start/Stop/Remove/Status onto the handoff-aware runtime lifecycle | #115, #116, #117 | ✅ Complete (#115 CreateContainer handoff prep, #116 StartContainer identity verification before Running, #117 Stop/Remove/Status handoff lifecycle) |
| CRI-I4 | Kubelet Validation | Validate the handoff-aware runtime through crictl, kubelet/k3s smoke, and restart/cleanup scenarios | #118, #119, #120 | 🟡 In progress (#118 built the gated crictl handoff-lifecycle fixture, both producer modes verified live on Apple Silicon; #119 wired the handoff path into the `macvz-cri` binary + handoff-aware in-loop harness, live kubelet smoke operator-pending; #120 validated restart recovery + orphan handoff/rootfs reclamation. Remaining: in-VM evidence producer + live kubelet/k3s smoke) |
| CRI-I5 | Hardening & Upstream Alignment | Harden permissions, draft upstream primitive proposal, and define the experimental feature gate/operator contract | #121, #122, #123 | ✅ Complete (#121 handoff permission hardening — chown-to-runAsUser narrowing with safe world-writable fallback, `docs/CRI_RUNTIME_I5_1_HANDOFF_PERMISSIONS.md`; #122 upstream primitive proposal drafted in `docs/CRI_RUNTIME_I5_2_UPSTREAM_PROPOSAL.md`; #123 experimental feature gate ratified + operator docs in `docs/CRI_EXPERIMENTAL_HANDOFF_OPERATOR.md`) |

| Issue | Phase | Task | Acceptance Focus |
| --- | --- | --- | --- |
| #109 | CRI-I1 | Add runtime handoff path lifecycle helper | ✅ Complete — `pkg/runtime/handoff.go` adds `HandoffManager` (`NewHandoffManager`/`Layout`/`Create`/`Cleanup`) owning the runtime-private path lifecycle. The canonical reserved-layout constants (`HandoffRuntimeRoot=/run/macvz`, `HandoffContainersRoot=/run/macvz/containers`, `HandoffMountPoint=/run/macvz/handoff`, `IdentityFile`) live in `handoffmeta.go` (#110) and are consumed here. Paths derive under `<root>/<containerID>/{rootfs,handoff}`; `sanitizeHandoffID` rejects empty IDs, separators, `..`, and shell metacharacters (`ErrInvalidHandoffID`). `Create` makes the container/rootfs dirs `0755` and forces the handoff dir to `0777` with `Chmod` so a restrictive umask cannot break non-root writes (R15 lesson); first-time create failures remove the newly staged subtree, while retry failures preserve any preexisting handoff state. `Cleanup` is idempotent (`RemoveAll`; missing subtree and invalid ID are no-ops). Hermetic — root is configurable so tests use `t.TempDir()`, no live apple/container. The helper exposes paths only as data; no kubelet/CRI mount is created here. `handoffmeta.go` (#110) consumes the derived `HandoffLayout` rather than re-deriving paths. Tests: `pkg/runtime/handoff_test.go` covers derivation, default-root, invalid/valid IDs, create modes under umask 0077, repeatable create, retry-failure preservation, subtree cleanup, missing-path/repeated cleanup, and invalid-ID cleanup no-op (`go test ./pkg/runtime/...` green; full `go build ./...`/`go test ./...` green). |
| #110 | CRI-I1 | Define runtime metadata for rootfs and handoff state | ✅ Complete — `pkg/runtime/handoffmeta.go` defines `HandoffMeta` (rootfs path, handoff path, identity file, expected/observed identity, `VerifiedAt`, `Status`, `Cleanup`) plus the `IdentityStatus` (`Pending`/`Verified`/`Mismatch`/`Missing`) and `CleanupState` (`Active`/`Stopped`/`Removed`) enums, and now owns the canonical reserved-layout constants (`HandoffRuntimeRoot`/`HandoffContainersRoot`/`HandoffMountPoint`/`IdentityFile`/`rootfsSubdir`/`handoffSubdir`) and the pure `HandoffPaths(id)` derivation that `handoff.go` (#109) consumes. `Verify(observed, now)` does exact-match identity resolution (no substring), `Verified()` gates Running, and `EffectiveStatus`/`EffectiveCleanup` map the zero value to the safe `Pending`/`Active`. Restart/recovery split is explicit: ID-derivable fields are reconstructable, `ExpectedIdentity` is recoverable from the prepared-rootfs spec, and `ObservedIdentity`/`Status`/`VerifiedAt`/`Cleanup` are best-effort (a restarted runtime re-establishes identity rather than trusting an unrecoverable result). No CRI store change; no Kubernetes API surface; no orphan recovery (non-goals honored). Documented in `docs/CRI_RUNTIME_I1_2_METADATA.md`. Tests: `pkg/runtime/handoffmeta_test.go` covers `HandoffPaths`↔`Layout` agreement, defaults, the exact-match `Verify` table, zero-value mapping, reconstruct-from-ID, and JSON round-trip (`go test ./pkg/runtime/...`, `go vet ./...`, full `go build ./...`/`go test ./...` green). Outcome `runtimeHandoffMetadataDefined`. |
| #111 | CRI-I1 | Guard kubelet mounts from runtime-private handoff path collisions | ✅ Complete — `translateMounts` rejects any kubelet mount whose cleaned `container_path` is at or under the reserved `/run/macvz` runtime namespace (covering the `/run/macvz/handoff` bind point) with `FailedPrecondition`, independent of the hostPath source allowlist and applied to tmpfs and bind mounts alike; segment-aware match keeps siblings like `/run/macvz-data` allowed. Tests: `TestCreateContainerRejectsReservedRuntimeMountTargets` plus existing mount-policy tests in `pkg/criserver/mounts_test.go` (`go test ./pkg/criserver/` green). |
| #112 | CRI-I2 | Build late-rootfs OCI spec handoff bind mount injection | ✅ Complete — `pkg/runtime/handoffmount.go` injects the runtime-private handoff bind mount described by CRI-R16. `HandoffMount(layout)` returns the MacVz realization of the R16 OCI bind mount `{type:bind, source:/run/macvz/containers/<id>/handoff, destination:/run/macvz/handoff, options:[rbind,rw]}` as a writable `types.Mount` (`ReadOnly=false`, `Tmpfs=false`); the apple/container driver maps that to a writable VirtioFS `--volume source:target`, keeping all apple/container/vminitd assumptions inside the runtime package. `InjectHandoffMount(spec, layout)` is a pure spec mutation that appends the mount, is idempotent (re-injecting the identical mount is a no-op), rejects incomplete/relative/out-of-namespace handoff targets, and rejects any pre-existing spec mount targeting the reserved `/run/macvz` namespace from a non-handoff source with `ErrHandoffMountConflict` (segment-aware, so `/run/macvz-data` is allowed) — the mount is always runtime-derived, never from a CRI/kubelet mount. `PrepareHandoffMountpoint(layout)` creates the in-rootfs destination `<RootfsDir>/run/macvz/handoff` through a rootfs-relative guest-path helper (idempotent, no `..` escape) and reports a clear error naming the offending path on failure. StartContainer identity verification is intentionally not wired here. Tests: `pkg/runtime/handoffmount_test.go` verify the OCI mount shape, append/idempotency, reserved-target conflict + sibling allow, bad-input guards, rootfs path cleaning, and destination create/failure (`go test ./pkg/runtime/...` green; full `go build ./...`/`go test ./...` green). |
| #113 | CRI-I2 | Implement late-rootfs identity file and handoff writer contract | ✅ Complete — `pkg/runtime/identity.go` implements the runtime identity contract. The runtime stages the *expected* identity into the prepared rootfs at `RootfsIdentityPath` (`<rootfs>/etc/macvz-container-identity`) via `StageIdentityFile`/`ReadStagedIdentity` (recovers `HandoffMeta.ExpectedIdentity` from the spec); staged/readback paths use the same rootfs-relative guest-path helper as #112. The launched process writes the *observed* identity into `HandoffLayout.IdentityFile`. Both use one line-oriented `key=value` format, split on the **first** `=` so R15's `identity=macvz-r9-id=late-alpha` (value contains `=`) round-trips. `ParseHandoffEvidence` matches keys exactly, ignores blanks/`#` comments, takes first-`identity`-wins (duplicates kept as `identity#2`), captures all other keys (`expected`/`proc_root`/`mounts`/`pid`/…) as first-seen-ordered diagnostics, and rejects `ErrEvidenceMalformed` (no separator / empty key) and `ErrEvidenceMissing` (no identity key / empty / absent file). `proc_root` and mount listings are diagnostics, never success criteria. `VerifyHandoffIdentity(meta, now)` records observed identity + result onto `meta` (exact match via #110 `Verify`) and returns `ErrIdentityMismatch` (wrapping expected+observed) / `ErrEvidenceMissing` (wrapping expected) with `meta.Status` set accordingly; nil/empty metadata returns a clear error instead of panicking. `ReadStderrDiagnostics` reads the optional `<handoff>/stderr` capture best-effort and size-bounded (16 KiB). Documented in `docs/CRI_RUNTIME_I2_2_IDENTITY_CONTRACT.md`. Tests: `pkg/runtime/identity_test.go` covers parser success/missing/mismatch/malformed/extra-diagnostics/duplicate, stage↔read round-trip, empty-rootfs guards, verify verified/mismatch/missing/malformed/bad metadata (errors carry expected/observed where useful), and stderr absent/present/bounded (`go test ./pkg/runtime/...`, `go vet ./...`, full `go build ./...`/`go test ./...` green). Non-goals honored (no CRI status presentation; `proc_root` not required to match a host path). Outcome `runtimeIdentityContractImplemented`. |
| #114 | CRI-I2 | Add gated LinuxPod integration for runtime handoff launch | ✅ Complete — `pkg/runtime/container/handoff_integration_test.go` adds `TestHandoffLaunchIntegration`, a gated live test that moves the R15 harness-only evidence proof onto the production helpers/spec shape. It stages the runtime-private layout with `runtime.HandoffManager` (#109), injects the handoff bind mount with `runtime.InjectHandoffMount` (#112), launches a real apple/container alpine workload whose VirtioFS-shared handoff dir is bind-mounted at the production `runtime.HandoffMountPoint` (`/run/macvz/handoff`), has the process write the `runtime.FormatIdentity` payload (#113) into the handoff file, and verifies identity with `runtime.VerifyHandoffIdentity` (#113, exact match; `proc_root`/`expected` are diagnostics only). Skipped unless `MACVZ_INTEGRATION=1`, so default `go test ./...` stays hermetic and never requires apple/container. Every stage logs a distinct `[outcome=…]` (service-unavailable, image-pull, prepare, create, start, process-failed, evidence-missing/malformed, identity-mismatch) so an operator report pinpoints failures; success logs `runtimeHandoffLaunchSucceeded`. **Live run (operator):** `MACVZ_INTEGRATION=1 go test ./pkg/runtime/container/ -run TestHandoffLaunchIntegration -v` on Darwin 25.5.0 arm64 + apple/container CLI 1.0.0 produced `outcome=runtimeHandoffLaunchSucceeded` (observed==expected `macvz-handoff-id=late-alpha`, `status=Verified`, PASS in 2.81s); full report `docs/CRI_RUNTIME_I2_HANDOFF_LAUNCH_REPORT.md`. Non-goals honored (no kubelet/k3s in-loop; production CRI route not enabled by default; vminitd late-rootfs primitive left to the gated Swift harness). Outcome `runtimeHandoffLaunchSucceeded`. |
| #115 | CRI-I3 | Map CRI CreateContainer to LinuxPod rootfs and handoff preparation | ✅ Complete — `pkg/criserver/container_handoff.go` wires the experimental LinuxPod handoff preparation into CreateContainer, gated by `Options.Handoff *runtime.HandoffManager` (nil keeps the default apple/container path unchanged). For the single-container path (excluded for joined multi-container), `prepareHandoff` follows the CRI-R16 CreateContainer ordering through runtime-owned helpers only: `HandoffManager.Create` (rootfs+handoff subtree), `PrepareHandoffMountpoint` (in-rootfs `/run/macvz/handoff` destination), `StageIdentityFile` (expected identity = workload ID, deterministic/recoverable), and `InjectHandoffMount` (writable handoff bind mount into the spec) — all before the workload is created, and it never starts the container. Image pull/verify is unchanged (ImageService or fallback Pull). Only the minimal CRI mapping is persisted: a `store.Container.HandoffPrepared` bool; paths are recomputed from `WorkloadID`. Failure handling cleans up: a prepare fault returns Internal/FailedPrecondition with no workload created and no record persisted; a workload-create or persist fault reclaims the workload and removes the handoff subtree (`handoffCleanup`). RemoveContainer's `cleanupHandoff` idempotently removes the subtree. Tests: `pkg/criserver/container_handoff_test.go` cover success (dirs/mountpoint/staged identity/injected mount/persisted marker, not started), default-path-unchanged, prepare-failure cleanup, persist-failure cleanup + reclaim, and RemoveContainer cleanup; existing CRI tests still green (`go test ./pkg/criserver/...`; full `go build ./...`/`go test ./...` green). No k3s/kubelet compatibility claim. |
| #116 | CRI-I3 | Map CRI StartContainer to identity verification before Running | ✅ Complete — `pkg/criserver/container_handoff_start.go` gates Running on handoff identity verification (CRI-R16). After the workload starts, `verifyHandoffIdentity` (inert unless `s.handoff != nil` and `c.HandoffPrepared`) derives the layout from the workload ID, reads the expected identity staged into the rootfs (`runtime.ReadStagedIdentity`, same source as #117's verbose status), and bounded-waits on the evidence file via the new `runtime.WaitForHandoffIdentity` (retries missing/empty evidence, terminal on mismatch/malformed, ctx-expiry wraps `ErrEvidenceMissing` naming the expected identity). Bounded by `handoffVerifyTimeout`/`handoffVerifyInterval` (defaults 30s/250ms; tests shorten). On any verification failure it unwinds the just-started workload via the generalized `unwindContainerStartReason` (stop + mark Exited, reason `IdentityVerificationFailed`, message includes the error and best-effort `ReadStderrDiagnostics`) and returns `FailedPrecondition`, so the container is never left Running. Persisting Running is also hardened to unwind (reason `PersistFailed`) on Put failure so a verified-but-unrecorded workload is not leaked. Network/log-pump ordering is preserved (verification sits between Start and Running). Documented in `docs/CRI_RUNTIME_I3_2_START_VERIFY.md`. Tests: `pkg/criserver/container_handoff_start_test.go` (verified→Running, mismatch, timeout, runtime-start-error, persist-failure, no-handoff passthrough) and `pkg/runtime/identity_test.go` (`WaitForHandoffIdentity` late-arrival/timeout/mismatch-terminal) — `go test ./pkg/criserver/...` & `./pkg/runtime/...`, `go vet ./...`, full `go build`/`go test ./...` green. Non-goals honored (no app readiness; no status semantics beyond launch identity). Outcome `startContainerIdentityGated`. |
| #117 | CRI-I3 | Map StopContainer, RemoveContainer, and Status to handoff lifecycle | ✅ Complete — completes the back half of the handoff-aware CRI lifecycle on top of the #115 create-time prep. **Stop preserves evidence:** `StopContainer`/`stopContainerRecord` record the exit state but never call `cleanupHandoff`, so the runtime-private `/run/macvz/containers/<id>` subtree (identity evidence, stderr) survives for post-mortem until removal. **Remove is idempotent:** `removeContainerRecord` calls `s.cleanupHandoff` (whose `runtime.HandoffManager.Cleanup` is a no-op on a missing subtree), so repeated removal and removal of an already-cleaned container both succeed. **Status verbose diagnostics:** new `pkg/criserver/handoff_lifecycle.go` `handoffStatusInfo` surfaces runtime-private identity-verification diagnostics (`handoffPrepared`, `handoffPath`, `handoffMountPoint`, `identitySource`, `expectedIdentity`, `observedIdentity`, `identityVerified`, `procRoot`) — read best-effort from the on-disk evidence channel via #109/#113 helpers (`ReadStagedIdentity`/`ReadHandoffEvidence`) — only under `ContainerStatus` Verbose; absent evidence degrades to `identityVerified=false`+`identityEvidence=missing` rather than erroring, and it is never part of the stable CRI status or a Kubernetes volume. **Reconcile:** unchanged by design — it advances a vanished workload to Exited from the runtime *phase* alone and never rereads identity (a start invariant, #110), so a started container stays verified through a runtime-not-found reconcile. Tests: `pkg/criserver/handoff_lifecycle_test.go` covers Stop-preserves-evidence, Remove cleanup idempotency (incl. repeat + direct cleanup of a gone subtree), verbose diagnostics (match + missing-evidence), non-verbose carries no Info, and runtime-not-found reconciliation; existing CRI lifecycle tests stay green (`go test ./pkg/criserver/`, `go build ./...`). Non-goals honored (handoff is not a Kubernetes volume; no multi-container Pod semantics). |
| #118 | CRI-I4 | Build local crictl fixture for LinuxPod handoff lifecycle | ✅ Complete — `test/e2e/cri-handoff/run.sh` (`make cri-handoff`, README) drives the handoff-aware adapter with `crictl` through the full CRI lifecycle (RunPodSandbox → Create → Start → Status → Stop → Remove) and asserts the CRI-R16 invariants: create-time handoff prep (`handoffPrepared=true`, staged `handoffPath`, staged `expectedIdentity`, #115), StartContainer's identity gate → Running (`identityVerified=true`, `observedIdentity==expectedIdentity`, #116), verbose-status diagnostics flattened by `crictl inspect` (#117), Stop **retains** the subtree, Remove **deletes** it, and idempotent cleanup (no stale container/sandbox/socket). The fixture also closed the binary gap shared with #119 — `cmd/macvz-cri/main.go` now wires `Options.Handoff` via `--experimental-handoff`/`--handoff-root` (off by default; `--handoff-root` targets a writable per-user dir since `/run/macvz/containers` isn't writable on macOS). Two identity-producer modes (`MACVZ_HANDOFF_PRODUCER`): `host-sim` (default) writes the expected identity into the host-visible handoff channel — standing in for the not-yet-wired in-VM late-rootfs producer — so the **full** lifecycle runs green; `none` skips it and asserts StartContainer's gate **times out** with the precise identity-evidence diagnostic, surfacing the in-VM-producer gap as the honest blocker for #119. Gated by `MACVZ_INTEGRATION=1` (plan-only + exit 0 otherwise, CI-safe). **Live evidence (2026-06-22, Apple Silicon, apple/container 1.0.0, busybox:1.36.1):** both modes ran end-to-end against a real micro-VM — host-sim all phases passed (`identityVerified=true`); none reproduced the `FailedPrecondition … handoff identity evidence missing … context deadline exceeded` timeout. `go build ./...`, `go vet`, `go test ./cmd/macvz-cri/... ./pkg/criserver/... ./pkg/runtime/...` green. Report `docs/CRI_RUNTIME_I4_1_HANDOFF_FIXTURE.md`. Non-goals honored (no k3s run; no multi-day stability claim). |
| #119 | CRI-I4 | Re-run kubelet/k3s in-loop fixture against handoff-aware runtime | ✅ Complete (precise blocker, honest) — closed the central code gap: the `macvz-cri` binary never set `Options.Handoff`, so no fixture could reach the handoff-aware lifecycle (#115..#117). Added `--experimental-handoff`/`--handoff-root` to `cmd/macvz-cri/main.go` (`run()` builds `runtime.NewHandoffManager` and passes `Options.Handoff`; off by default, shipped apple/container path unchanged; `--handoff-root` targets a writable per-user dir since `/run/macvz/containers` isn't writable on macOS). Made `test/e2e/cri-k3s/k3s-inloop.sh` handoff-aware: new `handoff` phase (gated `MACVZ_HANDOFF=1`) asserts a Pod reaching Running implies StartContainer's identity gate passed (#116) and, via `MACVZ_HANDOFF_STATUS_CMD`, surfaces on-node `handoffStatusInfo` (`identityVerified`/`expectedIdentity`/`observedIdentity`, #117). README node-setup documents enabling handoff on the adapter service. **Evidence:** handoff-enabled binary builds, exposes the flags in `--help`, and boots logging `experimental LinuxPod handoff path enabled` (wiring live, not just unit-tested); `go build ./...`, `go vet`, `go test ./cmd/macvz-cri/... ./pkg/criserver/... ./pkg/runtime/...` green; harness passes `bash -n` + plan-mode; the underlying handoff launch primitive already passed live in #114 (`runtimeHandoffLaunchSucceeded`). **Outcome `kubeletHandoffSmokeBlocked`:** the full kubelet-in-loop smoke needs a Linux k3s control plane + a macOS handoff-enabled CRI node (same operator-pending topology as #85); #119 removed the code blocker, the topology blocker remains. Report `docs/CRI_RUNTIME_I4_2_INLOOP_HANDOFF_REPORT.md`. Non-goals honored (no soak, no broad compatibility claim, handoff off by default). |
| #120 | CRI-I4 | Add restart and cleanup validation for handoff-aware containers | ✅ Complete — validated restart recovery and cleanup of runtime-private handoff state and closed the one real gap: orphan-subtree reclamation. New `runtime.HandoffManager.ListContainerIDs()` (`pkg/runtime/handoff.go`) enumerates well-formed per-container subtrees under the handoff root (missing root → empty; stray files/ill-formed names skipped). New `Server.sweepOrphanHandoffs` (`pkg/criserver/handoff_recover.go`), called from `RecoverContainers` after the reconcile loop (inert when handoff disabled), reclaims any subtree no surviving container record claims — left by a crash between staging a subtree and persisting its record, or a death mid-RemoveContainer — and reports reclaimed-vs-kept counts. It is conservative: a subtree claimed by any known container (Created/Running/Exited, prepared or not) is always kept, and identity evidence is never reread (orphan-ness is decided from the record set alone, matching the start-invariant model of #110/#117). **Hermetic tests** (`pkg/criserver/handoff_recover_test.go`): a restart-state matrix (Created, Running, Exited, failed Start → all kept and claimed; RemoveContainer after restart cleans the subtree and stays idempotent), orphan reclamation alongside a kept live subtree, reclaimed(2)/kept(1) accounting, inert-when-disabled, and removed-before-restart leaves no orphan + idempotent redundant remove; plus `pkg/runtime/handoff_test.go` `TestListContainerIDs*`. **Gated live scenario** `pkg/criserver/handoff_recover_integration_test.go` `TestLiveHandoffRestartCleanup` (`MACVZ_CRI_INTEGRATION=1`): real apple/container create → stage orphan → restart + recover → orphan reclaimed, live subtree kept → RemoveContainer cleans subtree and leaves no orphan workload. `go build ./...`, `go vet ./...`, `go test ./...` green. Report `docs/CRI_RUNTIME_I4_3_RESTART_CLEANUP_REPORT.md`. Non-goals honored (no long-duration soak; no non-CRI Virtual Kubelet change; no identity reread during recovery). Outcome `handoffRestartCleanupValidated`. |
| #121 | CRI-I5 | Harden handoff permissions and user mapping | ✅ Complete — narrowed the runtime-private handoff directory's permissions to the container's configured user while keeping non-root containers writable. `pkg/runtime/handoff.go` adds `HandoffOwner` (resolved `runAsUser`/`runAsGroup`) and `CreateForUser`: when the owner is known and the adapter can chown the handoff dir to it, the dir is chowned and narrowed to `0700` (uid only) or `0770` (uid+gid), removing the world-write bit; when the owner is unknown or the chown is not permitted (the common unprivileged-macOS case, since apple/container refuses root), it falls back to the `0777` world-writable mode so a non-root container can still write its identity evidence — correctness over hardening. `Create` is retained as the unknown-owner path. CRI wiring: `handoffOwnerFromConfig` (`container_handoff.go`) parses the security context (numeric `runAsUser`/`runAsGroup`; `runAsUsername` ignored as unreliable), `prepareHandoff`→`CreateForUser` threads it, and verbose `ContainerStatus` surfaces `handoffDirMode`+`handoffWritePolicy` (`owner-only`/`owner-group`/`world-writable-fallback`) for per-container confirmation. The container/rootfs dirs stay runtime-owned (`0755`); read-only-rootfs containers still write because the handoff bind mount is independently writable (`ReadOnly=false`); no host path is exposed and no user-namespace model is introduced (non-goals honored). **Tests:** `pkg/runtime/handoff_test.go` (narrow uid+gid→`0770`/uid-only→`0700` with owner asserted via a unix `statFileOwner` helper; foreign-uid chown EPERM→`0777` fallback, skipped as root; unknown-owner→`0777`); `pkg/criserver/container_handoff_test.go` (`handoffOwnerFromConfig` cases, end-to-end narrowing to the test uid/gid + verbose policy, read-only-rootfs-still-writable); `cmd/macvz-cri` `checkHandoff` documents the policy in preflight. `go build ./...`, `go vet ./...`, `go test ./pkg/runtime/... ./pkg/criserver/... ./cmd/macvz-cri/...` green. Report `docs/CRI_RUNTIME_I5_1_HANDOFF_PERMISSIONS.md`. Outcome `handoffPermissionsHardened`. |
| #122 | CRI-I5 | Draft upstream apple/containerization primitive proposal | ✅ Complete — `docs/CRI_RUNTIME_I5_2_UPSTREAM_PROPOSAL.md` drafts a concise upstream proposal grounded in the R9–R16 live evidence chain and pinned versions (base `6b7b42c`, `vminit:macvz-r12`, Swift 6.2.1, busybox:1.36.1, Darwin 25.5.0 arm64). It separates **required upstream changes** — a late-rootfs container primitive (`PrepareContainerRootfs` + late-binding `CreateContainer`, from R8) and two small `vmexec` rootfs-setup fixes (R11 ptmx ENOENT, R12 ensure /dev/null) referenced to the exact local patch files — from an **optional** vminitd-visible read-back primitive (the R14 finding) and from **MacVz-only policy that stays local** (handoff `/run/macvz` layout, identity format/exact-match, 0777 permission policy, bind-mount collision guard, CRI lifecycle mapping). Includes failure-mode and security-consideration tables and a "what upstream acceptance removes locally" matrix (Copy(COPY_IN) staging, carried R11/R12 patches, the R15 bind-mount workaround, the pinned initfs). Honors non-goals: no upstream PR opened, no dependence on upstream acceptance. Outcome `upstreamPrimitiveProposalDrafted`. |
| #123 | CRI-I5 | Decide experimental feature gate and operator documentation | ✅ Complete — ratified the experimental gate and documented it for operators. **Gate decision:** the single canonical gate is the `macvz-cri --experimental-handoff` CLI flag (+ `--handoff-root`), matching the explicit-CLI-flag convention of `--experimental-multi-container`; deliberately **no env-var gate** for the binary so enabling an experimental path is always visible in process args/service units (the e2e harness's `MACVZ_HANDOFF=1` gates a test phase, not the adapter). Flag help and the startup log now state experimental/off-by-default/not-the-VK-runtime and point to the operator doc. **Honest failure when disabled or unsupported:** new `prepareHandoffRoot` (`cmd/macvz-cri/main.go`) validates the handoff root at startup and **fails loudly before serving** (e.g. macOS default `/run/macvz/containers` read-only → clear `--experimental-handoff: … pass --handoff-root` error) instead of an opaque Internal error at first CreateContainer; no silent degrade to the non-handoff path when handoff was explicitly requested. New `checkHandoff` preflight check (`cmd/macvz-cri/preflight.go`, wired via `preflightConfig.hc`) reports disabled→OK (confirming the path is off), enabled+writable→OK, missing-root/writable-parent→WARN (created at start), and unusable-root/unwritable-parent→FAIL with the `--handoff-root` remedy. **Operator docs:** new `docs/CRI_EXPERIMENTAL_HANDOFF_OPERATOR.md` (experimental status, the gate, supported/unsupported behavior, honest-failure modes, build/preflight/run/cleanup). **README alignment:** `README.md` + `README.zh-TW.md` gain a short "Experimental tracks (not the shipped runtime)" note pointing to the operator doc, without altering the Virtual Kubelet node-provider positioning. Tests: `cmd/macvz-cri/preflight_test.go` (disabled/writable/missing-parent-writable/unwritable cases) and `main_test.go` `TestPrepareHandoffRoot` (creates writable root; fails loudly under a non-directory parent). `go build ./...`, `go vet ./...`, `go test ./...` green. Non-goals honored (CRI path not labeled production-ready; VK positioning unchanged). Outcome `experimentalHandoffGateDefined`. |

**CRI-P5 evidence (#77):** Pod networking is wired through the same primitives as
the shipped provider — `network.PodIPAM` for Pod IPs and `podnet.Router` for the
host pf binat path — reached via narrow interfaces in `pkg/criserver/network.go`.
`RunPodSandbox` reserves the Pod IP and rejects duplicate live sandboxes for the
same Pod key; `StartContainer` attaches the path once the micro-VM address
appears; `PodSandboxStatus.Network.Ip` and `Status.NetworkReady` are reported
only when actually ready; direct container stop/remove and self-exit reconcile
detach the path while sandbox remove releases the IP; teardown is idempotent and
`Server.RecoverNetwork` rebuilds reservations and re-attaches surviving sandboxes
after a restart. Hermetic coverage in `pkg/criserver/network_test.go`; gated live
smoke in `pkg/criserver/network_integration_test.go`
(`MACVZ_INTEGRATION=1 go test ./pkg/criserver -run 'Test.*Network|Test.*Sandbox'`).
`cmd/macvz-cri` exposes `--pod-cidr`/`--pod-network-interface` (networking off
until both are set). See [CRI_FEASIBILITY.md](CRI_FEASIBILITY.md) CRI-P5.

**CRI-P6 evidence (#78):** The kubelet-facing operational surfaces are honest over
the CRI path. Logs are file-based (`pkg/criserver/logs.go`): `StartContainer`
pumps the workload's follow stream into `<LogDirectory>/<LogPath>` in CRI format
(`<ts> stdout F <msg>`), with `ReopenContainerLog` for rotation. Exec/ExecSync and
PortForward (`pkg/criserver/streaming.go`) hand kubelet streaming URLs from
`k8s.io/kubelet/pkg/cri/streaming`; exec runs `container exec`, port-forward dials
the Pod micro-VM and proxies bytes. `Attach` returns a documented
`Unimplemented` (apple/container exposes no reattachable process stream). Stats
(`pkg/criserver/stats.go`) map the runtime `Stater` sample to
`ContainerStats`/`PodSandboxStats`, reporting attributes-only — never faked zeros —
when a sample is unavailable. Hermetic coverage in
`pkg/criserver/{streaming,logs,stats}_test.go`; gated live smoke in
`pkg/criserver/streaming_integration_test.go`
(`MACVZ_CRI_INTEGRATION=1 go test ./pkg/criserver -run 'Test.*Logs|Test.*Exec|Test.*PortForward|Test.*Stats'`).
`cmd/macvz-cri` exposes `--streaming-addr` (default `127.0.0.1:0`; exec/port-forward
return `FailedPrecondition` when empty). See
[CRI_FEASIBILITY.md](CRI_FEASIBILITY.md) CRI-P6.

**CRI-P7 evidence (#79):** Kubelet-driven Pod inputs and lifecycle behavior are
honest over the CRI path. In CRI mode the kubelet materializes projected
ConfigMap/Secret/Downward/SA-token and `emptyDir` content on the host and passes
them as bind mounts; `pkg/criserver/mounts.go` translates each into a
`types.Mount` (host bind or guest tmpfs for a Memory `emptyDir`) under a
conservative policy — mounts under the kubelet pods dir
(`--kubelet-pods-dir`, default `/var/lib/kubelet/pods`) are always allowed, any
other `hostPath` must be within a `--volume-host-path-allowed` prefix (empty
disables arbitrary hostPath), prefix matching is segment-aware, and bidirectional
propagation is rejected. Mounts are persisted and surface in
`ContainerStatus.Mounts`. Probes are kubelet-driven: HTTP/TCP from the kubelet
against the Pod IP (CRI-P5), exec via `ExecSync` (CRI-P6). `restartPolicy` is
honored by reporting exits faithfully and allowing recreate once the prior
container has Exited (only a live container blocks a new one).
`Server.RecoverContainers` (`pkg/criserver/recover.go`) reconciles persisted
containers against live workloads and resumes log pumps on restart without
duplicating or orphaning a workload, alongside `RecoverNetwork` for Pod IP/state.
Hermetic coverage in `pkg/criserver/{mounts,recover}_test.go`; gated live smoke in
`pkg/criserver/volumes_integration_test.go`
(`MACVZ_CRI_INTEGRATION=1 go test ./pkg/criserver -run 'Test.*Volume|Test.*Probe|Test.*Restart|Test.*Recovery'`).
Non-goals (multi-container shared volumes, subPath, dynamic PV) stay out of scope.
See [CRI_FEASIBILITY.md](CRI_FEASIBILITY.md) CRI-P7.

**CRI-P8 evidence (#80):** The experimental adapter is hardened into an
operator-facing k3s runtime path for single-container, non-host-namespace Pods.
`macvz-cri --preflight` (`cmd/macvz-cri/preflight.go`) reports runtime-dependency
status (apple/container CLI, socket, state dir, Pod networking, mount policy) as
`OK`/`WARN`/`FAIL` without mutating host state; its check logic is pure over
injectable probes and unit-tested. Unsupported Pod shapes
(`hostNetwork`/`hostPID`/`hostIPC`) are rejected by `RunPodSandbox` with a clear
`InvalidArgument` naming the spec field (`pkg/criserver/diagnose.go`).
`scripts/macvz-cri-install.sh` installs/uninstalls the adapter as a per-user
LaunchAgent — idempotent, preflighted, `KeepAlive`, with `uninstall`/`--purge`
leaving no stale socket/binary/state (`MACVZ_DRY_RUN=1` for rehearsal), and with
XML-escaped plist arguments plus `MACVZ_CRI_EXTRA_ARGS_FILE` for one-argument-per-line
extra flags when values contain spaces. The gated `test/e2e/cri-k3s/run.sh`
(`MACVZ_INTEGRATION=1`) drives the `crictl` compatibility suite — explicit
ImageService pull, lifecycle, logs, exec probe, projected config mount,
unsupported-shape rejection, adapter restart recovery, and cleanup verification —
and `test/e2e/cri-k3s/soak.sh` pulls once then runs a bounded create/delete soak
sampling adapter RSS with leak/orphan guards. k3s wiring is documented in
`test/e2e/cri-k3s/README.md`; `make cri-k3s`/`make cri-soak` are convenience
targets. Full `kubectl` fixture deployment and Service reachability remain CRI-P9
go/no-go evidence. No production-ready claim is made: the route-two go/no-go
(multi-container support, host-namespace workloads, real-hardware soak) is
deferred to CRI-P9. The Virtual Kubelet path is unchanged. See
[CRI_FEASIBILITY.md](CRI_FEASIBILITY.md) CRI-P8.

**CRI-P9 decision (#81):** The route-two go/no-go gate resolves to a **conditional
no-go for replacement, not a stop**: keep CRI as a documented experimental
`develop` side path; the shipped Virtual Kubelet provider remains the only
supported production runtime. The CRI adapter is honest and useful for the
single-container, non-host-namespace Pod class (P1–P8 have hermetic coverage plus
gated component/live harnesses; `go test ./...`, `go vet ./...`, `make build`,
`make cri` green), but it is still not a general node runtime. Follow-ups have
now narrowed the decision: #82/#86 prove the **current `apple/container` CLI path**
cannot model multi-container Pods by launching one independent micro-VM per
container, while #87 identifies `apple/containerization`'s experimental
`LinuxPod` API as the next route-C feasibility target; #84 clears
**host-namespace Pods** only by an honest scheduling-exclusion scheme, not by
pretending support; and #83/#85 separate CRI-socket soak evidence from real
kubelet/k3s in-loop evidence. Decision flips to **go** only after a LinuxPod-backed
Pod sandbox or equivalent runtime path proves Kubernetes Pod semantics and a
multi-day real k3s/kubelet in-loop soak passes; README positioning is unchanged
because the user-facing architecture does not change. Full decision package
(supported/unsupported workload shapes, gaps vs. Virtual Kubelet and vs.
ordinary CRI runtimes, k3s failure modes, operational + security model,
migration and fallback plans) is in [CRI_FEASIBILITY.md](CRI_FEASIBILITY.md)
CRI-P9.

**CRI-P9 follow-up (#82):** The multi-container blocker is confirmed
**architectural, not a missing flag**: `apple/container` runs one Linux kernel
per container, and a network namespace is per-kernel, so two micro-VMs cannot
share one. The exact missing primitive is *the ability to run a second OCI image
(own rootfs, lifecycle, limits) inside an existing Pod sandbox VM sharing that
VM's network namespace* — the pause-VM model used by Kata/Firecracker CRI
runtimes — recorded in code as `missingSharedNetnsPrimitive`. The adapter ships a
flag-gated path (`--experimental-multi-container`, `SharedPodNetworkRuntime` in
`pkg/criserver/multicontainer.go`): off by default it keeps the honest
one-container rejection; on, it admits a second container only if the runtime
implements `CreateInPodSandbox` (apple/container does not, so it rejects with a
diagnostic naming the gap). A hermetic test proves the adapter routes the second
container through that join operation with the first workload ID as the sandbox VM
target. An L3-shared-network approximation was rejected as dishonest (distinct
IPs break the single-Pod-IP/localhost contract). See
[CRI_FEASIBILITY.md](CRI_FEASIBILITY.md) "CRI-P9 Follow-up (#82)".

**CRI-P9 follow-up (#86):** Re-verified `apple/container` 1.0.0 CLI/runtime still
exposes no shared sandbox namespace (`--network "container:<id>"` → `network ...
not found`; no `pod`/`sandbox`/`pause` verb; no
`--net=container:`/`--pid`/`--ipc` flag), so the current CLI-backed CRI path
**stays blocked with evidence** and returns a clear `Unimplemented` diagnostic.
What #86 adds beyond #82 is the **adapter-side honest path** behind
`--experimental-multi-container`: container #2+ joins the **sandbox owner's** VM
via `CreateInPodSandbox` (never a second micro-VM), a joined container shares the
owner's **single Pod IP** with no second IP allocation or `binat` attach
(`store.Container.SharesPodNetwork`), per-container stop/remove keeps the Pod
network up **until the last container drains** (including owner-first stop order),
and a failed join leaks no record/IP/workload. Follow-up research found
`apple/containerization`'s experimental `LinuxPod` API, so the next step is no
longer "wait for `container` CLI support" or "self-build a runtime first"; it is
#87: validate `LinuxPod` as the route-C Pod sandbox backend and keep a MacVz-owned
sandbox runtime as fallback only. See [CRI_FEASIBILITY.md](CRI_FEASIBILITY.md)
"CRI-P9 Follow-up (#86)" and #87.

**CRI runtime feasibility (#87):** Route C is now the active research path:
validate `apple/containerization` `LinuxPod` before considering a MacVz-owned
sandbox runtime. C0 is complete in
[CRI_LINUXPOD_FEASIBILITY.md](CRI_LINUXPOD_FEASIBILITY.md): the decision is
**go to a minimal LinuxPod PoC**, not production adoption. `LinuxPod` is an
experimental Swift API that can place multiple Linux containers in one VM, with
separate rootfs/processes and shared VM CPU/memory/network; upstream integration
tests already cover multiple/concurrent containers, exec, stats, per-container
limits, filesystem isolation, optional shared PID namespace, and shared-network
sysctl evidence. The PoC must prove the Kubernetes-facing contract MacVz needs:
one Pod IP, localhost reachability across containers, sandbox lifetime
independent of any single container, kubelet-compatible
`RunPodSandbox`/`CreateContainer` ordering, logs/exec/stats, and the current
upstream gaps (`Attach`, `PortForward`, post-create `addContainer` hotplug). If
the PoC passes, create bridge implementation phases (likely a Swift helper daemon
controlled by the Go CRI adapter). #88 completed the first pure Swift
two-container shared-namespace PoC: one LinuxPod boots, two containers are
registered before `pod.create()`, localhost reaches across containers, exec and
stats work, and stopping the server first leaves the client observable. #89 then
tested kubelet-style ordering and confirmed `pod.addContainer` after
`pod.create()` returns `unsupported: "hotplug not supported"`. That means
LinuxPod can model predeclared multi-container Pods, but cannot currently provide
an honest general CRI backend for late sidecars/restarts without a deliberately
limited workload model or stop/recreate fallback. The PoCs do not yet prove vmnet
Pod IP attachment, `Attach`, `PortForward`, recovery, or k3s in-loop behavior.
#90 inspected the upstream hotplug path on `apple/containerization` 0.34.0:
post-create `addContainer` depends on `VirtualMachineInstance.hotplug`; the
default implementation returns `unsupported`, and the default
`VZVirtualMachineManager` path does not install a `HotplugProvider`. #91 then
installed a consumer provider and proved that post-create `addContainer` reaches
it; public VZ USB mass-storage attach succeeds, but no public API provides a
deterministic Linux guest block path for the attached ext4 rootfs, so MacVz
refuses to return a guessed `AttachedFilesystem`. Route C therefore should not
advance as a thin limited LinuxPod wrapper. #92 starts the deeper runtime
research track. The R0 decision is recorded in
[CRI_RUNTIME_R0_ARCHITECTURE.md](CRI_RUNTIME_R0_ARCHITECTURE.md): the target is
a true Pod VM runtime on Apple Silicon, including sandbox VM lifecycle,
guest-agent contract, deterministic rootfs hotplug/device discovery, CRI
mapping, networking, and recovery. The next smallest primitive is guest-side
hotplug device discovery, not a helper daemon or full runtime rewrite. See
[CRI_LINUXPOD_POC_REPORT.md](CRI_LINUXPOD_POC_REPORT.md),
[CRI_LINUXPOD_C2_REPORT.md](CRI_LINUXPOD_C2_REPORT.md),
[CRI_LINUXPOD_C3_DECISION.md](CRI_LINUXPOD_C3_DECISION.md),
[CRI_LINUXPOD_C4_REPORT.md](CRI_LINUXPOD_C4_REPORT.md),
[CRI_RUNTIME_R0_ARCHITECTURE.md](CRI_RUNTIME_R0_ARCHITECTURE.md), and issue #87.

**CRI-P9 follow-up (#84):** The host-namespace gate is **cleared via option (b),
an honest scheduling exclusion**. Honest host-namespace support is physically
impossible — a host namespace is per-kernel and each Pod is a separate micro-VM
kernel — so the answer is a kubelet-visible taint/label scheme (canonical keys in
`pkg/criserver/nodescheme.go`): labels `node.macvz.io/runtime=apple-container`
and `node.macvz.io/host-namespace=unsupported`, plus taint
`node.macvz.io/host-namespace-unsupported=true:NoSchedule`. Because system
DaemonSets often tolerate every taint, the scheme is layered: label-based
`nodeAffinity` exclusion for cluster-owned charts, and the existing loud
`RunPodSandbox` rejection (now naming the scheme) as the visible backstop for
charts you cannot edit — failing as a `FailedCreatePodSandBox` event, not
opaquely. The taint also makes MacVz an opt-in node for ordinary workloads:
compatible Pods must tolerate it and should select the runtime label.
`macvz-cri --preflight` prints the exact registration flags. See
[CRI_FEASIBILITY.md](CRI_FEASIBILITY.md) "CRI-P9 Follow-up (#84)".

**CRI-P9 follow-up (#85):** #83 intentionally stopped at a real-hardware
CRI-socket soak driven by `crictl`; it did not put a Linux kubelet/k3s control
plane in the loop. #85 **builds that missing layer**: a gated, operator-run
harness (`test/e2e/cri-k3s/k3s-inloop.sh`, `make cri-k3s-inloop`) plus a
single-container fixture (`test/e2e/cri-k3s/fixtures/workload.yaml`) that selects
the #84 runtime label and tolerates the host-namespace taint, and a runbook +
evidence template (`docs/CRI_K3S_INLOOP_REPORT.md`). The harness schedules the
fixture through a real k3s control plane and proves `kubectl rollout
status`/`logs`/`exec`/`port-forward`, ClusterIP/Service reachability from a
Linux-node probe, macvz-cri and k3s restart recovery, a soak (adapter RSS / Pod
restartCount / host workload counts), and a final orphan audit. It is gated
(`MACVZ_INTEGRATION=1` + reachable `KUBECONFIG`) and plan-only otherwise; restart
and audit phases use operator hooks and skip loudly when unset. The dev host
cannot stand up the Linux-control-plane + macOS-CRI-node topology unattended, so
the **live evidence is operator-pending**. Until that live run passes *and* #82
(multi-container) clears, CRI remains an experimental side path / no-go for
replacement even though #84 itself is resolved.

## Current Validation Snapshot

As of 2026-06-19, `main` has passed the two-node baseline described in
[MULTI_NODE_TEST_REPORT_2026-06-19.md](MULTI_NODE_TEST_REPORT_2026-06-19.md):

- two MacVz nodes register as Ready;
- Pods schedule to each Mac and clean up their micro-VMs;
- `logs`, `exec`, `port-forward`, metrics, and stats work through the kubelet API;
- Services publish EndpointSlices with one Ready endpoint per Mac;
- cross-node Service data-plane reachability remains blocked until the privileged
  WireGuard + `podNetwork` path is enabled and verified.

## Issue Tracker

| Issue | Title | Phase | Status |
| --- | --- | --- | --- |
| #1 | Initialize Go module and base project layout | P0 | closed |
| #2 | macvz-kubelet CLI entrypoint: flags, config loading, structured logging | P0 | closed |
| #3 | Define core package interfaces (runtime / provider boundaries) | P0 | closed |
| #4 | CI pipeline on macOS runner (build, vet, golangci-lint, test) | P0 | closed |
| #5 | Build & release tooling (Makefile, version stamping) | P0 | closed |
| #7 | Define and implement Runtime interface over apple/container | P1 | open |
| #8 | micro-VM lifecycle: start / stop / destroy | P1 | open |
| #9 | Log streaming from micro-VMs | P1 | open |
| #10 | Exec into running micro-VMs | P1 | open |
| #11 | Density & per-VM RAM-overhead benchmark | P1 | open |
| #12 | arm64 image pull verification | P1 | open |
| #13 | Wire Virtual Kubelet controller into macvz-kubelet | P2 | in progress |
| #14 | Register virtual node with capacity, addresses, taints, and conditions | P2 | in progress |
| #15 | Implement node heartbeat and lease updates | P2 | in progress |
| #16 | Implement Provider PodLifecycleHandler state and CRUD methods | P2 | in progress |
| #17 | Translate Kubernetes Pod specs into runtime workload specs | P2 | in progress |
| #18 | Wire kubectl logs and exec through the runtime | P2 | in progress |
| #19 | Add RBAC, manifests, and P2 MVP smoke test docs | P2 | in progress |
| #20 | Implement Kubernetes-coordinated Pod IPAM | P3 | closed |
| #21 | Bring up WireGuard mesh between MacVz nodes | P3 | closed |
| #22 | Connect micro-VM networking to the controllable Pod network path | P3 | closed |
| #23 | Report Pod IPs and readiness so Services resolve across MacVz nodes | P3 | closed |
| #24 | Implement kubectl port-forward for MacVz-backed Pods | P3 | closed |
| #25 | Implement node and pod metrics reporting | P4 | closed |
| #26 | Support VirtioFS-backed volumes for MacVz Pods | P4 | closed |
| #27 | Handle image architecture and Rosetta-for-Linux behavior | P4 | closed |
| #28 | Harden mTLS, RBAC, and runtime access boundaries | P4 | closed |
| #29 | Add signed and notarized macvz-kubelet release flow | P4 | closed |
| #30 | Build multi-node end-to-end test suite for beta readiness | P4 | closed |
| #37 | Run full WireGuard + podNetwork two-Mac e2e with privileged networking | P5 | planned |
| #38 | Add a privileged network helper daemon for WireGuard, route, sysctl, and pf operations | P5 | planned |
| #39 | Define and implement the local Unix-socket API between macvz-kubelet and the network helper | P5 | done — versioned control API (`pkg/network/privhelper`: protocol negotiation, `status`/`exec` ops, structured `APIError` codes, 1 MiB request cap); kubelet surfaces helper status at startup ([cmd/macvz-kubelet/main.go](../cmd/macvz-kubelet/main.go)); tests in `control_test.go`; spec in [PRIVILEGED_NETWORKING.md](PRIVILEGED_NETWORKING.md#control-api-kubelet--helper-39) |
| #40 | Add launchd install/uninstall support for the privileged network helper | P5 | in progress |
| #41 | Restrict helper inputs to configured CIDRs, interfaces, peers, and pf anchors | P5 | in progress |
| #42 | Add mesh peer reconciliation for adding/removing MacVz nodes without full restart | P5 | in progress |
| #43 | Extend e2e diagnostics for WireGuard handshakes, routes, pf anchors, and forwarding state | P5 | in progress |
| #44 | Document full privileged networking setup and recovery procedures | P5 | done — [docs/PRIVILEGED_NETWORKING.md](PRIVILEGED_NETWORKING.md) |
| #45 | Support restartPolicy Always and controller-managed workload expectations | P6 | in progress — restart loop + docs in [docs/WORKLOADS.md](WORKLOADS.md) |
| #46 | Support ConfigMap-backed environment variables and volume mounts | P6 | in progress — env + volume projection, docs in [docs/WORKLOADS.md](WORKLOADS.md) |
| #47 | Support Secret-backed environment variables and volume mounts | P6 | in progress — `secretKeyRef`/`envFrom secretRef` env + read-only `secret` volume projection (items, modes, optional), values never logged; tests in `pkg/provider/secrets_test.go`, docs in [docs/WORKLOADS.md](WORKLOADS.md) |
| #48 | Support envFrom, valueFrom, fieldRef, and resourceFieldRef translation | P6 | in progress — unified env resolver covers `envFrom` precedence, literal `$(VAR)` expansion, `fieldRef` metadata/spec paths, and `resourceFieldRef` CPU/memory/ephemeral-storage divisors; tests in `pkg/provider/downward_test.go`, fixture coverage in [test/e2e/p6-compat](../test/e2e/p6-compat/) |
| #49 | Support imagePullSecrets and private registry authentication | P6 | in progress — dockerconfigjson pull secrets resolved per Pod, registry login/pull/logout in the driver, docs in [docs/WORKLOADS.md](WORKLOADS.md) |
| #50 | Implement readiness, liveness, and startup probe handling | P6 | in progress — exec/HTTP/TCP probes gate readiness, restart on liveness failure, startup gating; docs in [docs/WORKLOADS.md](WORKLOADS.md) |
| #51 | Improve ServiceAccount token projection and in-cluster API compatibility | P6 | in progress — projected kube-api-access volume (bound token via TokenRequest, cluster CA, namespace) materialized at the standard path, docs in [docs/WORKLOADS.md](WORKLOADS.md) |
| #52 | Define supported and unsupported securityContext behavior for MacVz Pods | P6 | in progress — field-by-field policy: maps `runAsUser`/`runAsGroup`/`readOnlyRootFilesystem`/`capabilities` onto runtime flags, accepts VM-isolation no-ops, rejects `privileged`/`seLinux`/`Localhost` seccomp+appArmor/`procMount`/`sysctls` terminally; tests in `pkg/provider/securitycontext_test.go` + `pkg/runtime/container/driver_test.go`, model in [README](../README.md) and [docs/WORKLOADS.md](WORKLOADS.md) |
| #53 | Build a multi-Deployment compatibility fixture for rollout validation | P6 | in progress — P6 acceptance workload under [test/e2e/p6-compat/](../test/e2e/p6-compat/) (web + checker Deployments, ConfigMap/Secret/probe/ServiceAccount/Downward coverage), `run.sh` validates rollout/status/logs/exec/Service with redacted diagnostics; `make compat` + CI `compat` job |
| #54 | Create a node bootstrap/join command or documented workflow | P7 | in progress — `macvz-kubelet bootstrap` renders a node config from the minimum join inputs (kubeconfig, node name, internal IP, pod CIDR, mesh address/peers) with optional WireGuard keypair + self-signed serving TLS; `macvz-kubelet doctor` preflights runtime, kubeconfig/API reachability, wireguard tooling, privileged helper, pf/forwarding, and serving-TLS validity, naming each missing prerequisite; workflow in [docs/NODE_JOIN.md](NODE_JOIN.md); tests in `pkg/bootstrap/` |
| #55 | Automate WireGuard key generation, public key exchange, and config rendering | P7 | in progress — `macvz-mesh` helper (`make mesh`): `keygen` (stable 0600 key), `export` (public-only node metadata, derives key/endpoint), `peer` (renders `mesh.peers:` YAML or wg `[Peer]` blocks that round-trip into config); rotation documented in [docs/NETWORKING.md](NETWORKING.md); tests in `pkg/config/metadata_test.go` + `pkg/network/wireguard/config_test.go` |
| #56 | Add node health and readiness diagnostics across runtime, provider, mesh, and pod network | P7 | in progress — `pkg/health` aggregates checks into a single report that distinguishes control-plane (registration + node-lease freshness), runtime (apple/container readiness), and data-plane (privileged helper, WireGuard mesh/routes, IP forwarding, pod attachments) failures, with text + JSON rendering and a ready/not-ready verdict that names the blocking class+check; served live from the kubelet at `/healthz/diagnostics` (200 ready / 503 not ready, `?format=json`) wired from the running components; complements join-time `macvz-kubelet doctor` (#54). Aggregation/checker tests in `pkg/health/`, wiring tests in `cmd/macvz-kubelet/diagnostics_test.go` |
| #57 | Add node drain and safe workload cleanup guidance/tooling | P7 | in progress — `kubectl drain` flow + expected controller rescheduling documented in [docs/NODE_DRAIN.md](NODE_DRAIN.md); per-Pod teardown already destroys VMs + detaches pod-network rules via `DeletePod`; new `macvz-kubelet cleanup` reaps orphan `macvz-*` micro-VMs (API-aware, `--dry-run`/`--all`) and flushes the pf anchor for post-drain/post-crash verification; `runtime.Lister`/`Driver.List` + exported `provider.WorkloadID`; tests in `pkg/drain/` |
| #58 | Add node removal workflow, including route, peer, pf, and VM cleanup | P7 | in progress — `macvz-kubelet remove` runs the departing node's permanent teardown in order, best-effort + idempotent: delete Node object → reap all MacVz micro-VMs (reuses #57 `pkg/drain`) → flush pf anchor → tear down WireGuard routes+interface (`wireguard.Mesh.Remove`); `--yes` safety gate, `--dry-run`/`--keep-node`, partial-failure reporting; remaining-node peer pruning stays a documented SIGHUP step. Orchestration in `pkg/noderemove` (+tests), wiring in `cmd/macvz-kubelet/remove.go` (+tests); runbook + recovery in [docs/NODE_REMOVAL.md](NODE_REMOVAL.md), ops section in [docs/MULTI_NODE_OPS.md](MULTI_NODE_OPS.md) |
| #59 | Produce a local diagnostic bundle command for support and bug reports | P7 | in progress — `macvz-kubelet bundle` collects config (loaded + raw), node object, recent events, live health report (#56), runtime status/containers/images, helper status, routes, IP forwarding, WireGuard interface, and pf anchor rules into a timestamped directory + tar.gz; every source is best-effort (a failing source records the error instead of aborting). All output passes through `pkg/diagbundle`'s redactor — PEM private keys, WireGuard private/preshared keys, JWT/bearer tokens, and the values of a curated sensitive-key set are stripped, while public material (certs, public keys, CA data) is kept for debugging. Redaction is the security chokepoint (Builder-level, so new sources cannot leak); tests in `pkg/diagbundle/` (redaction + packaging), docs in [docs/DIAGNOSTIC_BUNDLE.md](DIAGNOSTIC_BUNDLE.md) |
| #60 | Document multi-node operations, failure modes, and recovery playbooks | P7 | in progress — operator lifecycle runbook in [docs/MULTI_NODE_OPS.md](MULTI_NODE_OPS.md) covers join, verify, drain, remove, upgrade, network troubleshooting, and cleanup with commands + expected output; it is the order-of-operations index that ties together [NODE_JOIN.md](NODE_JOIN.md) (#54), the `macvz-mesh` peer workflow (#55), and the live `/healthz/diagnostics` report (#56), and routes each failure class to the per-symptom recovery procedures in [PRIVILEGED_NETWORKING.md](PRIVILEGED_NETWORKING.md). Drain (#57), automated removal (#58), and a diagnostic-bundle command (#59) are documented as the manual procedures operators run today, flagging where future tooling will replace a step. README + README.zh-TW link the runbook. Validation: two-Mac walkthrough (join→drain→remove with peer-list pruning). |
| #61 | Run a minimal public HTTP application and expose it through a browser-visible Service | P8 | in progress — `test/examples/hello-http/`: stock public `nginx:1.27-alpine` (arm64, no custom build/registry auth) Deployment fronted by a ClusterIP Service, serving a per-Pod page rendered from a ConfigMap via the Downward API so single-node vs. multi-node load-balancing is visible on refresh. Browser-visible path is `kubectl port-forward svc/hello` (no kube-proxy/LoadBalancer; proxies into the micro-VM via `pkg/provider/portforward.go`). `run.sh` harness applies, waits for rollout, asserts HTTP 200 + page body over a real port-forward, checks logs, then tears down (`MACVZ_HELLO_KEEP=1` leaves the namespace for manual browser smoke; `MACVZ_HELLO_REPLICAS=N` for multi-node spread). README documents browser verification + manual cheatsheet. Live bring-up on a kind control plane + single MacVz node proved node registration (`macvz-local` Ready) and an nginx micro-VM booting with a vmnet IP; surfaced and fixed two hard blockers only a live podNetwork run reveals: (1) the vmnet default-route guard crashed the kubelet at cold start (`route delete ... bad address: bridge100`) before any micro-VM created the bridge — now tolerated in [pkg/network/podnet/router.go](../pkg/network/podnet/router.go) (`TestStartToleratesMissingVMNetInterface`); (2) the always-present `default/kubernetes` Service (host-network apiserver, outside pod/vmnet CIDRs) poisoned the shared pf anchor and failed **every** Pod's network attach — the svcroute controller now filters Service backends to MacVz-routable CIDRs ([pkg/network/svcroute/controller.go](../pkg/network/svcroute/controller.go) + `Config.RoutableServiceCIDRs`, `TestReconcileFiltersUnroutableBackends`). Remaining: final HTTP-200-over-port-forward capture (automated in `run.sh`), deferred — the dual-NIC dev Mac matches the finding-#1 host-route-churn hazard, so the browser smoke should run on the two-Mac rig or a dedicated node. |
| #62 | Run a guestbook-style application with multiple Deployments, Services, ConfigMaps, and Secrets | P8 | in progress — `test/examples/guestbook/`: a multi-tier guestbook on two stock public arm64 images (no custom build/registry auth) — `redis-leader` (1) + `redis-follower` (2, `replicaof` the leader) data tier and a `frontend` (3) busybox `httpd`+CGI web tier. Three Deployments, three Services, two ConfigMaps (`APP_TITLE`/`app.conf` env + the landing page/CGI), one Secret (Redis password as env on Redis and as a read-only file on the frontend, #46/#47). The CGI speaks Redis RESP over `nc`: reads (`LRANGE`) from the follower replica, writes (`RPUSH`) to the leader, so the page proves replication across Deployments + ClusterIP routing (#37). Browser path is `kubectl port-forward svc/frontend` (no kube-proxy/NodePort). `run.sh` harness asserts rollout/availability, follower `master_link_status:up`, a real port-forward browser smoke (landing page + submit-an-entry write→leader/read←follower + HTML-escaping), logs banner, exec exit-code fidelity, `scale` 3→5→3, `rollout restart` with state surviving in Redis, and clean namespace teardown (no Pods/VMs left) — with redacted diagnostics. Wired into the #65 catalog + `run-all.sh`. Validation: full data path validated end-to-end with docker against the exact images (`redis:7-alpine`+`busybox:1.36.1`) — replication, urlencoded form posts, leader/follower split, ordering, HTML-escaping, AUTH enforcement; manifests offline-validated (3 Deploy/3 Svc/2 CM/1 Secret, namespace re-targeting); `run.sh`/`run-all.sh` pass `bash -n`. Live two-Mac browser smoke pending a running cluster. |
| #63 | Evaluate and run a Kubernetes management UI such as Headlamp or Dashboard on MacVz | P8 | in progress — evaluation in [docs/MANAGEMENT_UI.md](MANAGEMENT_UI.md) selects **Headlamp** over the Kubernetes Dashboard (single arm64 container vs. the Dashboard's multi-container gateway/scraper split, which MacVz's one-container-per-micro-VM model rejects). Fixture `test/e2e/headlamp-ui/`: hardened (restricted-PSS) single-replica Deployment using the projected SA token (#51) + ClusterIP routing to `kubernetes.default` (#37) for in-cluster API, HTTP readiness/liveness probes (#50), bound to the read-only `view` ClusterRole for RBAC-limited interaction. Browser path is `kubectl port-forward svc/headlamp` + a minted SA login token (no kube-proxy → no NodePort/LoadBalancer). `run.sh` asserts rollout/placement/probe-stability, projected-token mount, a real port-forward HTTP smoke of `/config`+SPA, and the RBAC boundary (`can-i list` yes / `delete`,`create`,`update` denied), with redacted diagnostics + teardown. Surfaced follow-ups: ingress/exposure without port-forward, multi-container UI support, and metrics-server panels. Validation: manifests offline-validated (YAML/selectors/labels/ports, render+image-override pipeline); live MacVz browser+RBAC smoke pending a running cluster. |
| #64 | Define and run a CBB arm64-compatible subset on MacVz | P8 | in progress — scoping/compatibility analysis in [docs/CBB_VALIDATION.md](CBB_VALIDATION.md). CBB = Cloud-Barista (`CB-`prefixed Go microservices). Defines the supported subset as the **stateless control services** (CB-Spider, CB-Tumblebug, CB-MapUI) — each single-container, wired by ClusterIP routing without kube-proxy (#37), config/secrets via #46–#49, probes via #50, browser access via `kubectl port-forward` (#28), with CB-Tumblebug pointed at an **external** key-value/RDB store so the on-node subset stays stateless. Unsupported components listed with reasons + required MacVz features: CB-Dragonfly monitoring stack and any in-cluster etcd/MariaDB/InfluxDB need dynamic PV provisioning + StatefulSet support (MacVz volumes are ephemeral VirtioFS, #26); amd64-only images need confirmed multi-arch builds; GPU/NVIDIA and hostPath/privileged/host-network agents stay on regular Linux nodes (#52); NodePort/LoadBalancer exposure blocked by no-kube-proxy (shared follow-up with #63). Scoped per the user's steer to a roadmap/blocker analysis rather than standing up a live CBB service; runnable manifests + expected outputs deferred to #65. Validation: component shapes classified against MacVz's feature set; no CBB service deployed (per scope), live subset smoke pending a running cluster. |
| #65 | Publish real-app validation manifests and expected outputs | P8 | in progress — published catalog [`test/examples/README.md`](../test/examples/README.md) + suite runner [`test/examples/run-all.sh`](../test/examples/run-all.sh) consolidating the complete, assertable P8 fixtures (`hello-http` #61, `p6-compat` #53, `headlamp-ui` #63, `guestbook` #62) into one repeatable entry point. `run-all.sh` runs each fixture's self-contained apply→assert→teardown harness in sequence (cheapest first), aggregates a pass/fail summary, supports id subsets (`./run-all.sh hello-http p6-compat`), `--list`, and suite-wide pass-through knobs (`KUBECONFIG`/`KUBECTL`/`MACVZ_E2E_TIMEOUT`/`MACVZ_E2E_CONTINUE`); exits non-zero if any fixture fails, while each fixture attempts its own namespace teardown before returning. README documents per-fixture expected output (pods, services, logs, browser behavior, pass line, cleanup) so failures are localizable, plus a "when a fixture fails" diagnostics guide; the guestbook fixture (#62) is now published and wired in, leaving the CBB subset (#64) as the remaining not-yet-published fixture. Validation: all four fixtures' manifest sets pass `kubectl apply --dry-run=client`; every `run.sh` plus `run-all.sh` pass `bash -n`; `run-all.sh` `--list`/`--help`/unknown-id/orchestration smoke-tested. Live clean-cluster `run-all.sh` pending a running MacVz cluster. |
| #66 | Implement kubelet restart recovery for existing apple/container workloads | P9 | in progress — micro-VMs outlive the kubelet process, so on restart the provider re-adopts running workloads instead of rebuilding them. `CreatePod` ([pkg/provider/pod.go](../pkg/provider/pod.go)) gained an adoption fast-path: it probes the runtime for the Pod's deterministic `WorkloadID` (`lookupWorkload`) and, when the micro-VM is already there, skips pull/create, records the existing workload, starts it only if recovery finds it stranded in Created, re-attaches the Pod network path, and resumes probing. Pod IPs stay stable because `RecoverAllocations` (#20) already reserves each Pod's recorded `Status.PodIP` before the controller runs. Adoption avoids a needless re-pull that could now fail (e.g. rotated `imagePullSecret`) and strand a healthy container; the driver's idempotent `Create` is the backstop so a missed probe can't duplicate a VM, and `unwindCreate` leaves an adopted VM running if a mid-recovery re-attach fails. Tests in `pkg/provider/pod_test.go` (adopt-without-pull/create/start for Running VMs; start a recovered Created VM; restart a recovered Stopped VM; adoption succeeds when the image can no longer be pulled); docs in [docs/WORKLOADS.md](WORKLOADS.md#kubelet-restart-recovery-66). Validation: `go test ./...`/`go vet ./...` green; live restart smoke (kubelet restart → Pod back to Running, same IP, no new VM) pending a running cluster. Orphan VMs whose Pod is gone are out of scope here (#67). |
| #67 | Add orphan micro-VM detection and cleanup policy | P9 | in progress — automatic in-kubelet orphan reaper in [pkg/orphan](../pkg/orphan/reaper.go): a periodic loop that destroys MacVz micro-VMs whose backing Pod no longer exists on this node (missed delete, or a Pod removed while the kubelet was down), reclaiming pinned CPU/RAM/disk. It is the continuous counterpart to restart-adoption (#66) and the operator-run `cleanup`/`remove` sweeps (#57/#58): it reuses `drain.Cleaner` Scan/Reap but adds a **grace period** (a VM must stay continuously orphaned across `gracePeriod` before reaping, so it never races Pod creation/adoption) and **fail-safe** behaviour (reaps nothing when the live-Pod set can't be determined). Expected-set source is the node-scoped Pod informer cache (no extra API calls; Pods being deleted or in unsupported init/ephemeral/multi-container shapes excluded); only `macvz-`prefixed VMs are ever touched. It deliberately does **not** flush the pf anchor (the node is live). Config `orphanCleanup{enabled,interval,gracePeriod,dryRun}` (default on, 2m/10m) in [pkg/config](../pkg/config/config.go) with validation; wired in [cmd/macvz-kubelet](../cmd/macvz-kubelet/serve.go) after the pod controller syncs; docs in [docs/ORPHAN_CLEANUP.md](ORPHAN_CLEANUP.md) + config.example.yaml. Tests in `pkg/orphan/reaper_test.go` (grace gating, grace reset when a Pod returns, dry-run, fail-safe on lister error, failed-destroy retry, non-MacVz VMs untouched) and config interval validation. Validation: `go build ./...`/`go vet`/targeted `go test` green; live orphan-after-crash smoke pending a running cluster. |
| #68 | Improve node and Pod resource accounting for CPU, memory, disk, and image cache usage | P9 | in progress — extends the #25 CPU/memory metrics with **disk and image-cache accounting**. New optional `runtime.DiskReporter` capability (`pkg/runtime/disk.go`: `FilesystemUsage`, `ImageCacheUsage`, `ErrDiskUsageUnavailable`) implemented by the apple/container driver (`pkg/runtime/container/disk.go`): node filesystem via `statfs(2)` on configurable `runtimeDataRoot` (falling back to the operator home directory; `statfs_unix.go`/`statfs_other.go`, injectable seam), image-cache size+count by summing `container image ls --format json` (tolerant of numeric/string/variant sizes). The metrics collector now populates the Summary API's node `fs` (capacity/used/available + inodes, for disk-pressure eviction) and `runtime.imageFs.usedBytes`, plus five Prometheus gauges (`macvz_node_filesystem_{capacity,used,available}_bytes`, `macvz_image_cache_bytes`, `macvz_image_cache_images`); each surface degrades independently (`DiskSample` per-field ok). `BuildNode` advertises node `ephemeral-storage` capacity/allocatable from the filesystem total/available (operator-set values preserved; caller map never mutated). Disk stays node-scoped — no per-Pod ephemeral attribution yet. Tests: `pkg/metrics/disk_test.go`, `pkg/runtime/container/disk_test.go`, `pkg/provider/node_test.go`; docs in [docs/METRICS.md](METRICS.md). Validation: `go test ./...`/`go vet ./...` green; live `kubectl top`/`statfs` numbers pending a running cluster. |
| #69 | Add log rotation and structured diagnostics for long-running nodes | P9 | in progress — log locations, rotation, and structured-logging conventions defined in [docs/LOGGING.md](LOGGING.md). The privileged helper runs under launchd `KeepAlive` and grew `/var/log/macvz-netd*.log` without bound; `macvz-netd install` now also drops a `newsyslog` rotation config (`/etc/newsyslog.d/macvz-netd.conf`, `DefaultNewsyslogPath`) so macOS's `com.apple.newsyslog` rotates both logs (size-driven ~5 MB, 7 bzip2 archives; tunable via `LogRotateCount`/`LogRotateSizeKB`, opt out by clearing `NewsyslogPath`), and `uninstall` removes it. Rendering is pure (`RenderNewsyslog`) mirroring the plist pattern; install/uninstall wired in [pkg/network/privhelper/launchd.go](../pkg/network/privhelper/launchd.go), surfaced in the install log line. Kubelet bounding documented two ways (klog `--log-file`/`--log-file-max-size`, or stderr redirect + newsyslog). Structured diagnostics already use `klog.*S` with workload/pod (`pod`,`workloadID`,`podIP`,`phase`), node, and helper-op (`name`,`args`,`exit`) context (probes at `V(2)`); conventions tabulated in the doc. Tests in `pkg/network/privhelper/newsyslog_test.go` + extended launchd install/uninstall. Validation: `go test ./pkg/network/privhelper/...` green; live long-running rotation smoke pending a running node. |
| #70 | Build install, upgrade, rollback, and uninstall packaging for macvz-kubelet and helper | P9 | in progress — unified installer [`scripts/macvz-install.sh`](../scripts/macvz-install.sh) packaging both long-running pieces as managed macOS services: `macvz-netd` (root LaunchDaemon, delegated to its own `install`/`uninstall`) and `macvz-kubelet` (per-user LaunchAgent, since `apple/container` refuses root). Versioned layout (`<prefix>/libexec/macvz/versions/<v>` + `current`/`previous` symlinks) makes upgrade reversible: `install` stages binaries + seeds `<etc>/config.yaml` only if absent + starts both services; `upgrade` flips `current` (recording `previous`) and reloads, preserving config/PKI/state; `rollback` returns to `previous` and is itself reversible; `uninstall [--purge]` removes services+binaries and optionally `<etc>`. Release wired up: [scripts/macos-release.sh](../scripts/macos-release.sh) now builds+signs **both** binaries and emits a self-contained `macvz_<version>_darwin_arm64` install bundle (binaries + installer + config template + packaging doc) alongside the single-binary tarball, with both binaries submitted for notarization. Operator guide [docs/PACKAGING.md](PACKAGING.md); `make install-rehearsal`. Validation: a no-root rehearsal [`scripts/macvz-install-rehearsal.sh`](../scripts/macvz-install-rehearsal.sh) drives install→upgrade→rollback→uninstall→purge in a temp prefix (launchctl/netd stubbed) and asserts symlink state, reversible rollback, and config preservation — all green; `make release` builds the signed bundle end-to-end (ad-hoc) and the bundle tarball contains both binaries + installer; scripts pass `bash -n`. Live install/upgrade/rollback/uninstall rehearsal on a real Mac (services started, node joins/drains) pending. |
| #71 | Run long-duration soak tests across kubelet/helper restarts and node churn | P9 | in progress — added the P9 soak harness [`test/e2e/soak/run.sh`](../test/e2e/soak/run.sh) plus operator guide [docs/SOAK_TESTS.md](SOAK_TESTS.md). The harness loops the existing multi-node e2e suite and optionally the P8 real-app catalog, captures before/after node resource and Summary API snapshots, cordon/uncordon churns a MacVz node each iteration, and uses operator-provided command hooks to restart kubelet/helper services, verify #66 restart recovery (same PodIP after kubelet restart), create an orphan while kubelet is stopped and verify #67 reaps it after the grace window, and run #57 cleanup dry-run as a final no-orphan check. `make soak` is the entry point. Validation: `bash -n test/e2e/soak/run.sh` green; live multi-hour run on the two-Mac rig pending. |
